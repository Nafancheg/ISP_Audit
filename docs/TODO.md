# ISP_Audit — TODO (Открытые проблемы и задачи)

Дата: 15.01.2026 (актуализировано)
Назначение: единый список нерешённых проблем и задач. Всё реализованное и историческое остаётся в Changelog и архитектурном документе.

---

## Приоритеты
- 🔴 Критический: влияет на корректность детекции/обхода
- 🟡 Важный: улучшает точность/надёжность
- 🟢 Низкий: UX/полиш, интеграции

---

## Сводка (актуально)
- ✅ Фаза 1 (детекция + PID + базовые стратегии) выполнена: retransmissions, HTTP redirect, RST inspection, fail counter/time window, PID-гейтинг.
- ✅ “Переезд на v2” закрыт по ключевым гейтам: v2 не зависит от `GetSignals(...)` в рантайме, UI принимает только `[V2]` рекомендации, есть guard-тесты.
- ✅ Для реального браузинга в v2 включён QUIC fallback: подавление QUIC через DROP UDP/443 + метрика дропа (делает TCP/TLS bypass применимым для YouTube-подобных целей).
- 🟡 HTTP Host tricks: базовая реализация есть в `BypassFilter`, но пока не интегрирована в policy-driven слой/semantics (не управляется как FlowPolicy и не видна как policy-метрики).
- ⏳ По стратегиям обхода остаются блокеры/долги: Bad checksum (2.2 — есть блокер на уровне движка/инжекта), QUIC obfuscation (2.3) — в текущем виде покрывается `DropUdp443`, но как отдельная стратегия/политика требует явного оформления.
- 🟡 Auto-hostlist подключён в v2 как контекст (evidence/notes), но пока не влияет на постановку диагноза/плана.
- 🔴 **P0.0–P0.3 — приоритетные задачи**:
  - P0.0: Стабилизация качества обхода
  - P0.1: Транзакционность и наблюдаемость (Accumulative Attachment Model)
  - P0.2: Policy-Driven Execution Plane
  - P0.3: **Декомпозиция God-файлов** — 9 файлов >750 строк, суммарно ~12,000 строк для разбиения
- 📋 **Policy‑Driven Execution Plane**: целевая модель зафиксирована в архитектуре (см. `ARCHITECTURE_CURRENT.md`, раздел про policy-driven), а план внедрения — в P0.2 ниже.
---

## P0 — Стабилизация обхода (КРИТИЧЕСКОЕ)

- 🔴 P0.0: Деградация качества обхода после усложнения сценариев
  - Симптом: ранние версии проекта чаще достигали рабочего обхода; текущая сложность v2/сценариев и «шум» приводят к ситуации «применяю — не помогает/хаос в целях».
  - Цель: вернуть предсказуемый и эффективный обход на базовых кейсах (в первую очередь браузерный YouTube/Google), без побочных эффектов на систему.
  - Критерии приёмки (definition of done):
    - Базовый кейс: YouTube видео стабильно стартует в браузере при включённом обходе (админ, WinDivert активен).
    - Наблюдаемость: в UI есть подтверждение активного воздействия (например рост `Udp443Dropped` при QUIC→TCP).
    - Безопасность: после закрытия приложения DNS/DoH возвращаются в исходное состояние.
  - Ближайшие шаги (без “весовой модели” сейчас):
    - Зафиксировать 1–2 эталонных сценария воспроизведения (браузер/домен/провайдер/режим QUIC).
    - Regression bisect: найти коммит/период, где «работало», и сравнить отличия в `BypassFilter`/`TlsBypassService`/DNS.
    - Ввести режим “classic/минимальный набор” (фиксированный набор техник, минимум автоматики), чтобы вернуть baseline.
    - После стабилизации — перейти к улучшениям ранжирования/доминирования планов (см. ниже как follow-up).

- 🔴 P0.1: Транзакционность и наблюдаемость «применения обхода» (Accumulative Attachment Model)
  - Контекст/проблема:
    - Сейчас пользователь видит «где-то применено», но не видит состав операции и к чему именно применено (какая цель/карточка/hostKey, какие режимы, какие фильтры, какой набор IP/портов).
    - Из‑за этого возникает ощущение «переключателя между карточками» и непонимание, что реально активно прямо сейчас.
  - Цель:
    - Сделать «применение обхода» транзакцией с полным снимком конфигурации и привязкой к **группе целей** (Service Group), где карточки/hostKey — участники.
    - Целевая реализация: группа представляет собой **пакет политик (policy bundle)**, которые выбирают действие как функцию от пакета (dst_ip/proto/port/markers). Аккумуляция используется для endpoint-наборов/TTL‑блоков/assist‑флагов.
    - В любой момент времени можно ответить: «что применено? куда? почему именно так? что должно измениться в трафике/метриках? что пошло не так, если не сработало?».
  - Термины (для UI/лога):
    - «Применение обхода» = transaction apply (не использовать голое слово “Apply”).
    - «Карточка» = строка результата (hostKey/target).
    - «Группа целей» = сервис (YouTube/Steam/…): набор карточек, которые должны работать одновременно.
    - «Вклад (attachment)» = вклад карточки в конфигурацию группы (endpoint-ы/флаги/требования).
  - Что именно нужно показывать (чёткая «картинка транзакции»):
    - Заголовок: TransactionId, время, инициатор (ручное действие / авто по правилу), причина (коротко), целевая карточка (hostKey + отображаемое имя), статус выполнения (Started/Applied/Failed/RolledBack).
    - Область применения (Scope): per-group (Service Group) + per-policy (dst_ip_set/proto/port/markers) + явный список endpoint-ов (IPv4/IPv6), на которые реально нацелены политики/фильтры.
    - Конфигурация движка и режимов:
      - Активный пресет/параметры фрагментации (если применимо).
      - Assist-флаги: QUIC→TCP (DROP UDP/443), No SNI (ALLOW_NO_SNI) и т.п.
      - DNS/DoH стратегия (если включается).
    - Конфигурация фильтров (структурно): список включённых IPacketFilter с приоритетами и коротким назначением (что именно режем/разрешаем).
    - «Фактические входные данные», на которых транзакция построена:
      - Какие IP были известны (observed/resolved), откуда (DNS cache / SNI cache / resolve сейчас), TTL/возраст, что было отброшено из-за ограничений (cap/ttl/invalid).
    - Ожидаемый эффект/инварианты (чтобы было понятно «что должно измениться»):
      - Пример: при включённом QUIC→TCP и наличии endpoint-ов должен расти счётчик Udp443Dropped именно по IP цели.
      - Пример: при включённом TLS bypass должны появляться метрики по обработанным TCP/443 и корректный ActivationStatusSnapshot.
    - Диагностические подсказки «почему может не сработать» (как минимум на уровне текста):
      - Нет endpoint-ов → селективный DROP UDP/443 не воздействует.
      - Цель ушла в IPv6, а endpoint-ы только IPv4 (или наоборот).
      - Нет SNI, а AllowNoSni выключен.
      - Трафика нет → ACTIVATED, но NO_TRAFFIC.
  - UI/UX (где показывать «картинку»):
    - В каждой карточке:
      - Индикатор группы (GroupKey) + краткое резюме EffectiveGroupConfig (1 строка: стратегия + assist-флаги + endpoints=N).
      - Индикация статуса выполнения последней транзакции для группы (Applied/Failed + время) + признак «эта карточка участвует / исключена».
      - Кнопки per-card: «Применение обхода», «Ретест», «Переподключить» (reconnect‑nudge) + мини‑индикатор ретеста.
    - Панель «Детали применения обхода» (по выбранной карточке):
      - Разворот/expander/side panel, который показывает полный снимок последней транзакции (структурно, как выше).
      - Кнопка «Скопировать в буфер» (текст/JSON) для репорта.
    - Лог:
      - Печатать 1 краткую строку на транзакцию + по запросу (expander) разворачивать детали.
  - Модель данных (как источник истины):
    - Ввести единый объект «транзакция применения обхода» (условно: BypassApplyTransaction):
      - Request: groupKey, initiatorHostKey, desired plan/flags, reason, cancellation/timeout.
      - Snapshot (что реально применили): EffectiveGroupConfig + contributions + filters/options + resolved endpoints + derived expectations.
      - Result: success/fail + ошибка + rollback outcome + timings.
    - Ввести состояние группы (условно: GroupBypassAttachmentSet):
      - groupKey → набор attachments (по hostKey) + LastAppliedTransactionId + EffectiveGroupConfig.
      - Защита от перетирания ручных attachments фоновыми событиями (диагностика может обновлять диагноз/план, но не должна бесшумно менять состав группы).
    - Журналирование:
      - Хранить N последних транзакций в памяти + сохранять последнюю (или последние K) в LocalAppData для дебага.
  - Сериализация/«транзакционность»:
    - Жёстко сериализовать операции применения обхода (очередь/lock), чтобы не было гонок и частично применённых состояний.
    - Аккумулятивная модель по умолчанию:
      - «Группа целей = пакет политик + аккумулятивные endpoint-наборы/флаги».
      - Новое применение по карточке обновляет вклад (attachment) карточки и пересобирает: PolicyBundle + EffectiveGroupConfig (endpoints/flags), не выключая остальные карточки группы.
      - Merge-правила должны быть детерминированными (endpoints=union, assist-flags=OR, политики по приоритетам).
    - UI должен показывать:
      - EffectiveGroupConfig,
      - какие карточки сейчас участвуют (contributions),
      - откуда взялись endpoints (DNS/SNI/resolve) и их возраст.
  - Ретест и переподключение (per-card):
    - Ретест запускается только для выбранной карточки и пишет результат/статус в эту карточку, не затрагивая остальные.
    - Переподключение (nudge reconnect) также привязано к карточке: блокировать только endpoint-ы этой карточки (TTL) и сразу/параллельно запускать её ретест.
  - Критерии приёмки (Definition of Done):
    - В UI для выбранной карточки доступна панель деталей с полным снимком последней транзакции.
    - В логе на каждую транзакцию есть TransactionId и компактная строка состава (strategy + flags + endpoint-count).
    - Невозможно получить состояние «кнопки переключаются, не понятно что применено»: всегда видно активный policy bundle группы и какие политики реально матчились/применялись.
    - Per-card ретест/переподключение не зависят от видимости bypass‑панели и доступны во время диагностики.
  - План реализации по шагам (выполнять строго по порядку):
    - Шаг 1: Зафиксировать продуктовый режим: поддерживаем несколько активных групп одновременно (Steam + YouTube), а Decision Graph выбирает действие по признакам пакета.
    - Шаг 2: Определить контракт данных для Transaction (Request/Snapshot/Result) и вкладов (contributions) + формат лог-строк.
    - Шаг 3: Вынести «применение обхода» в отдельный сервис уровня Core/Bypass (не UI), который возвращает TransactionResult + Snapshot.
    - Шаг 4: Добавить in-memory журнал транзакций (N=50) и сохранение последней транзакции на диск (для репорта).
    - Шаг 5: Добавить сбор «фактических endpoint-ов» в одном месте (DNS+SNI+resolve) и включить в Snapshot.
    - Шаг 6: Добавить вычисление «ожидаемых эффектов/инвариантов» (ожидания по метрикам) и включить в Snapshot.
    - Шаг 7: Протянуть per-card состояние участия в группе (contribution/participation) в VM карточек: показывать краткое резюме активного policy bundle и время/статус последней транзакции группы.
    - Шаг 8: Перенести кнопки per-card (применение обхода/ретест/переподключение) в UI карточек + индикаторы статусов per-card.
    - Шаг 9: Реализовать панель «Детали применения обхода» по выбранной карточке + copy-to-clipboard (текст/JSON).
    - Шаг 10: Убрать/задепрекейтить глобальные элементы управления, которые создают впечатление глобального переключателя (или явно пометить их как «активно для группы X»).
    - Шаг 11: Догейтить: логика диагностики не имеет права менять ручной состав policy bundle, только предлагать изменения.
    - Шаг 12: Добавить smoke/регресс-гейт: «последняя транзакция сохраняется», «UI показывает policy bundle/срабатывания», «per-card ретест работает во время диагностики».

- 🔴 P0.2: План внедрения Policy‑Driven Execution Plane (см. `ARCHITECTURE_CURRENT.md`, Design 15.01.2026)
  - Цель: уйти от «глобальной стратегии» к детерминированному выбору стратегии как функции от пакета, сохранив текущий движок и безопасность.
  - Принцип: внедрять по протоколам/портам (UDP/443 → TTL‑block → TCP/80 → TCP/443), с feature‑gate и быстрым откатом.
  - **Маппинг policy-driven модели → текущая реализация (As-Is → Target)**:
    | Концепция policy-driven модели | Текущая реализация (As-Is) | Целевое состояние (Target) |
    |----------------------|---------------------------|---------------------------|
    | FlowPolicy | Глобальные флаги в `BypassProfile` + один `TlsStrategy` | Формальная модель `FlowPolicy` с приоритетами |
    | Decision Graph | `StandardStrategySelectorV2` (последовательный выбор) | Компиляция политик → O(1) lookup по признакам пакета |
    | Execution Plane | Один `BypassFilter` на весь TLS/443 | Per-protocol handlers с lookup в Decision Graph |
    | Semantic Groups | Domain families в UI (концепция) | Policy bundle с merge-правилами и UI статусами |
    | LOCAL/GLOBAL scope | Селективный UDP/443 через `ObservedTargetIpCache` | FlowPolicy.Scope с явными правилами приоритетов |
  - Этапы (минимально инвазивно):
    - [ ] **Этап 0**: Типы FlowPolicy/DecisionGraphSnapshot + компилятор + hard‑conflict валидация
      - Подзадачи:
        - 0.1: Создать `Core/Models/FlowPolicy.cs` (Id, Match, Action, Scope, Priority, TTL, CreatedAt)
        - 0.2: Создать `Core/Models/MatchCondition.cs` (DstIpSet, Proto, Port, TlsStage, SniPattern)
        - 0.3: Создать `Core/Models/PolicyAction.cs` (enum: Pass, Block, Strategy + параметры)
        - 0.4: Создать `Core/Models/DecisionGraphSnapshot.cs` (ImmutableDictionary lookup)
        - 0.5: Создать `Core/Bypass/PolicySetCompiler.cs` (валидация конфликтов, компиляция в snapshot)
        - 0.6: JSON-схема для FlowPolicy (для экспорта/импорта)
      - Gate: DPI2-040 — компилятор корректно детектирует hard-конфликты; поведение не меняется (zero runtime impact)
    - [ ] **Этап 1**: UDP/443 (QUIC→TCP) как политики (LOCAL/GLOBAL), observability matched/applied
      - Подзадачи:
        - 1.1: Рефакторинг ветки `BypassFilter.ShouldDropUdp443(...)`/`BypassFilter.Process(...)` — lookup в DecisionGraph вместо глобального флага
        - 1.2: Генерация FlowPolicy из `DropUdp443` + `ObservedTargetIpCache` при Apply
        - 1.3: Метрики per-policy: `matched_count`, `applied_count` (не только глобальный `Udp443Dropped`)
        - 1.4: Feature gate: `PolicyDrivenUdp443` (выключен по умолчанию, включается для smoke)
      - Gate: DPI2-041 — UDP/443 drop работает через политику; метрики per-policy корректны; fallback на старую логику при выключенном gate
    - [ ] **Этап 2**: TTL endpoint block (reconnect‑nudge) как TTL‑политика с самым высоким приоритетом
      - Подзадачи:
        - 2.1: FlowPolicy с TTL и Priority=MAX для блокировки endpoint-а
        - 2.2: Автоматическое удаление политики по истечении TTL
        - 2.3: UI индикатор «endpoint заблокирован до HH:MM:SS»
      - Gate: DPI2-042 — TTL-политика корректно истекает; приоритет выше любых других политик
    - [ ] **Этап 3**: TCP/80 Host tricks как политика (включает существующую реализацию)
      - Подзадачи:
        - 3.1: Рефакторинг HTTP Host tricks → FlowPolicy Action
        - 3.2: Интеграция с Decision Graph
      - Gate: DPI2-043 — HTTP Host tricks работают через политику; нет регрессий
    - [ ] **Этап 4**: TCP/443 TLS ClientHello policy‑выбор стратегии (per-domain/per-endpoint), fallback на текущую логику
      - Подзадачи:
        - 4.1: Рефакторинг ветки `BypassFilter.Process(...)` для TCP/443 ClientHello (isClientHello + dstPort==443) — lookup в DecisionGraph
        - 4.2: Per-domain стратегии: разные `TlsStrategy` для разных endpoint-ов одновременно
        - 4.3: Fallback на `TlsBypassService.CurrentStrategy` при отсутствии политики
        - 4.4: Метрики per-policy для TLS-стратегий
      - Gate: DPI2-044 — per-domain стратегии работают; fallback корректен; метрики per-policy
    - [ ] **Этап 5**: Semantic Groups как «пакет политик» + UI статусы ENABLED/PARTIAL/NO_TRAFFIC
      - Подзадачи:
        - 5.1: Модель `SemanticGroup` (GroupKey, DisplayName, DomainPatterns, PolicyBundle)
        - 5.2: Merge-правила для политик группы (endpoints=union, flags=OR, по приоритетам)
        - 5.3: UI статусы группы: ENABLED / PARTIAL (часть политик не matched) / NO_TRAFFIC
        - 5.4: Интеграция с P0.1 (Accumulative Attachment Model)
      - Gate: DPI2-045 — группа YouTube с несколькими доменами показывает корректный статус
    - [ ] **Этап 6**: UI таблица политик + экспорт snapshot (репорт)
      - Подзадачи:
        - 6.1: UI таблица активных политик (Id, Match, Action, Scope, Priority, Matched, Applied)
        - 6.2: Экспорт DecisionGraphSnapshot в JSON для репорта
        - 6.3: Advanced режим: ручное создание/редактирование политик (опционально)
      - Gate: DPI2-046 — таблица отображает все активные политики; экспорт валидный JSON
  - **Риски и ограничения** (для учёта при реализации):
    - Overhead на lookup в Decision Graph — измерить latency на hot path
    - Память для хранения политик — ограничить количество (cap)
    - Latency при перекомпиляции графа — делать async, не блокировать UI
  - **Edge-cases для документирования**:
    - ClientHello без SNI (ECH/ESNI) — политика с `TlsStage=NoSni` или fallback
    - IPv6 — селективность пока недоступна, явно документировать
    - Несколько активных групп одновременно — merge-правила должны быть детерминированными

- 🔴 P0.3: Декомпозиция God-файлов (КРИТИЧЕСКОЕ — сокращение издержек разработки)
  - Цель: разбить файлы >800 строк на модули с чёткими границами ответственности, чтобы:
    - Уменьшить когнитивную нагрузку при изменениях
    - Ускорить навигацию и code review
    - Снизить риск регрессий при правках
    - Упростить тестирование отдельных компонентов
  - Принцип: начинать с самых больших файлов; partial classes как промежуточный шаг; затем выделение в отдельные классы/сервисы.
  - **Реестр God-файлов** (по убыванию размера):

    | Файл | Строки | Приоритет | Статус |
    |------|--------|-----------|--------|
    | `DiagnosticOrchestrator.cs` | 2235 | 🔴 P1 | План в Задаче 4.3 |
    | `BypassController.cs` | 1758 | 🔴 P1 | Новый план ниже |
    | `TestResultsManager.cs` | 1478 | 🟡 P2 | Частично в 4.4 |
    | `MainViewModelRefactored.cs` | 1440 | 🔴 P1 | Новый план ниже |
    | `BypassStateManager.cs` | 928 | 🟡 P2 | Новый план ниже |
    | `TlsBypassService.cs` | 874 | 🟡 P2 | Новый план ниже |
    | `BypassFilter.cs` | 829 | 🟡 P2 | Новый план ниже |
    | `LiveTestingPipeline.cs` | 788 | 🟢 P3 | Новый план ниже |
    | `DnsSnifferService.cs` | 756 | 🟢 P3 | Можно оставить |

  - [ ] **P0.3.1: BypassController.cs** (1758 строк → цель <400 на файл)
    - Текущее состояние: смешивает UI-биндинги, bypass lifecycle, метрики, события смены сети.
    - План декомпозиции:
      - `BypassController.Core.cs` (partial) — публичный API: Enable/Disable/Apply, свойства для биндинга
      - `BypassController.Metrics.cs` (partial) — сбор и форматирование метрик, UI-обновления счётчиков
      - `BypassController.NetworkEvents.cs` (partial) — обработка смены сети, revalidation flow
      - `BypassController.Presets.cs` (partial) — логика пресетов фрагментации, assist-флаги
    - Далее (после partial): выделить `IBypassMetricsCollector`, `INetworkChangeHandler` как отдельные сервисы
    - Gate: GOD-001 — функциональность не изменилась; smoke bypass проходит

  - [ ] **P0.3.2: MainViewModelRefactored.cs** (1440 строк → цель <400 на файл)
    - Текущее состояние: god-ViewModel со всей логикой UI, командами, состоянием диагностики/bypass.
    - План декомпозиции:
      - `MainViewModel.Commands.cs` (partial) — все ICommand (StartDiagnostic, StopDiagnostic, ApplyBypass, etc.)
      - `MainViewModel.DiagnosticState.cs` (partial) — состояние диагностики, progress, результаты
      - `MainViewModel.BypassState.cs` (partial) — состояние bypass, метрики, UI-обновления
      - `MainViewModel.Navigation.cs` (partial) — табы, модальные окна, панели
    - Далее: выделить `IDiagnosticStateService`, `IBypassUiState` как отдельные классы
    - Gate: GOD-002 — UI работает без изменений; smoke ui проходит

  - [ ] **P0.3.3: TestResultsManager.cs** (1478 строк → цель <400 на файл)
    - Текущее состояние: парсинг сообщений, управление карточками, состояние результатов, v2 recommendations.
    - План декомпозиции:
      - `TestResultsManager.Parsing.cs` (partial) — парсинг pipeline messages, извлечение статусов
      - `TestResultsManager.Cards.cs` (partial) — создание/обновление карточек, группировка
      - `TestResultsManager.V2Recommendations.cs` (partial) — трекинг v2 рекомендаций, маппинг в UI
      - `TestResultsManager.State.cs` (partial) — in-memory состояние результатов
    - Далее: выделить `IPipelineMessageParser`, `ICardFactory` как отдельные сервисы
    - Gate: GOD-003 — карточки отображаются корректно; smoke pipe проходит

  - [ ] **P0.3.4: BypassStateManager.cs** (928 строк → цель <500 на файл)
    - Текущее состояние: SSoT для bypass, но также включает observed IP cache, журнал сессий, watchdog.
    - План декомпозиции:
      - `BypassStateManager.Core.cs` (partial) — основной SSoT: Apply/Disable, текущее состояние
      - `BypassStateManager.ObservedIpCache.cs` (partial) — кеш observed IP с TTL
      - `BypassStateManager.SessionJournal.cs` (partial) — журнал сессий, crash recovery
      - Watchdog уже вынесен или минимален
    - Gate: GOD-004 — bypass SSoT работает; smoke dpi2 проходит

  - [ ] **P0.3.5: TlsBypassService.cs** (874 строк → цель <500 на файл)
    - Текущее состояние: управление TrafficEngine, регистрация фильтров, TLS parsing, стратегии.
    - План декомпозиции:
      - `TlsBypassService.Engine.cs` (partial) — lifecycle TrafficEngine, Start/Stop
      - `TlsBypassService.Filters.cs` (partial) — регистрация/дерегистрация фильтров
      - `TlsBypassService.TlsParsing.cs` (partial) — парсинг ClientHello, SNI extraction (→ потом в Rust)
    - Gate: GOD-005 — TLS bypass работает; smoke bypass проходит

  - [ ] **P0.3.6: BypassFilter.cs** (829 строк → цель <400 на файл)
    - Текущее состояние: hot path обработки пакетов, фрагментация, disorder, fake, UDP drop.
    - План декомпозиции:
      - `BypassFilter.Core.cs` (partial) — основной Process(), маршрутизация по типу пакета
      - `BypassFilter.TlsStrategies.cs` (partial) — Fragment/Disorder/Fake логика
      - `BypassFilter.UdpHandling.cs` (partial) — UDP/443 drop, QUIC detection
      - `BypassFilter.Metrics.cs` (partial) — счётчики, BypassMetrics
    - Gate: GOD-006 — bypass фильтр работает; бенчмарк не деградировал

  - [ ] **P0.3.7: LiveTestingPipeline.cs** (788 строк → цель <400 на файл)
    - Текущее состояние: orchestration тестирования, но также включает sniffer, tester, classifier логику.
    - План декомпозиции:
      - `LiveTestingPipeline.Orchestration.cs` (partial) — Start/Stop, progress, cancellation
      - `LiveTestingPipeline.SnifferStage.cs` (partial) — интеграция с TrafficCollector
      - `LiveTestingPipeline.TesterStage.cs` (partial) — вызовы StandardHostTester
      - `LiveTestingPipeline.ClassifierStage.cs` (partial) — вызовы StandardBlockageClassifier
    - Gate: GOD-007 — pipeline работает; smoke pipe проходит

  - **Порядок выполнения** (по приоритету и зависимостям):
    1. P0.3.1 (BypassController) — много UI-зависимостей, часто редактируется
    2. P0.3.2 (MainViewModel) — центральный ViewModel, блокирует другие UI-задачи
    3. P0.3.6 (BypassFilter) — hot path, подготовка к Rust (Фаза 5)
    4. P0.3.4 (BypassStateManager) — SSoT, важен для P0.1
    5. P0.3.5 (TlsBypassService) — подготовка к Rust (Фаза 5)
    6. P0.3.3 (TestResultsManager) — зависит от P0.3.2
    7. P0.3.7 (LiveTestingPipeline) — наименее критичен

---

## Фаза 1 — Ядро: детекция + PID + стратегии (Критический приоритет)
**Статус:** ✅ Выполнено (v0.5.2-alpha)

Примечание: детали реализации и текущая архитектура — в `ARCHITECTURE_CURRENT.md`, история — в `CHANGELOG.md`.

---

## Фаза 2 — Расширение обхода (Средний приоритет)
**Статус:** ⏳ В ожидании (есть отложенные стратегии/блокеры)

### Фаза 2 (v2) — Доработки после MVP (Signals → Diagnosis → Strategy → Executor)
**Статус:** ✅ Закрыто по smoke-гейтам (dpi2/pipe strict)

- [x] Задача 2.V2.1: Реальный Executor (ручной запуск, без auto-apply) (🔴)
  - Цель: заменить MVP-логирование на реальное применение плана ТОЛЬКО по явному действию пользователя.
  - Минимальный контракт:
    - Команда/кнопка: «Применить рекомендации v2».
    - Преобразование `BypassPlan` → вызовы `BypassController`/`TlsBypassService`.
    - Отмена через `CancellationToken`, таймаут, безопасный откат/Disable.
  - Gate:
    - Без клика пользователя ничего не применяется.
    - Лог содержит понятный `Reasoning` + список стратегий.

- [x] Задача 2.V2.2: Feedback Store (🟡)
  - Цель: запоминать исход (успех/неуспех) для пары (DiagnosisId + StrategyId) и использовать дальше для ранжирования.
  - MVP формат: in-memory + сохранение в файл (опционально) с ограничением размера.
  - Gate:
    - При отсутствии данных поведение полностью как сейчас.
    - Данные не растут бесконечно (TTL/лимит записей).

- [x] Задача 2.V2.3: Ранжирование по feedback (🟡)
  - Цель: поверх hardcoded приоритетов добавлять вес по успешности.
  - Gate:
    - Жёсткие защиты сохраняются: `confidence < 50` → пустой план; `RiskLevel.High` запрещён при `confidence < 70`.
    - Детерминизм: одинаковый вход + одинаковый feedback → одинаковый план.

- [x] Задача 2.V2.4: Усилить сенсоры под DPI-диагнозы (🟡)
  - Цель: чтобы `ActiveDpiEdge/StatefulDpi` опирались на устойчивые улики (RST TTL delta/latency, инъекции), а не только флаги.
  - Gate:
    - На «рабочих» целях не появляются уверенные DPI-диагнозы.
    - На «проблемных» целях уменьшается доля `Unknown`.

- [x] Задача 2.V2.5: Auto-hostlist (2.4) — участие в v2 (🟡)
  - Цель: подключить auto-hostlist как источник фактов/контекста для v2 (evidence/notes, далее — вход в диагноз).
  - Gate:
    - Нет регрессий производительности.

- [x] Задача 2.V2.6: Зафиксировать отложенные стратегии и блокеры (🟡)
  - HTTP Host tricks (2.1) — deferred
  - QUIC obfuscation (2.3) — deferred
  - Bad checksum (2.2) — deferred (есть блокер на уровне движка)

- [x] Задача 2.V2.7: Переезд v2 на SignalSequence-only (без `BlockageSignals`) (🔴)
  - Цель: v2 runtime‑цепочка строится по событиям `SignalSequence` + `InspectionSignalsSnapshot`, без `IBlockageStateStore.GetSignals(...)` и без legacy‑классификатора.
  - Подзадачи:
    - Сенсоры/адаптер пишут факты напрямую в `SignalSequence`.
    - В runtime‑пути v2 нет `GetSignals(...)`, `legacySignals.*`, `StandardBlockageClassifier`.
    - `BlockageSignals` не допускается в v2 runtime‑пути (hard purge legacy/bridge).
  - Доп. критерии (после MVP, «полное вычищение legacy/bridge», ✅ выполнено):
    - [x] Убрано `InspectionSignalsSnapshot.FromLegacy(...)` и прекращено создание `BlockageSignals` в v2 smoke/адаптерах.
    - [x] Удалены bridge-слои: v2 сбор/диагноз/план не содержат ссылок на `BlockageSignals`.
    - [x] Guard `DPI2-025` ужесточён: список `allowedBlockageSignalsFiles` пустой.
  - Gate:
    - Smoke `DPI2-025` проходит (guard на отсутствие legacy в v2 runtime‑пути).

- [x] Задача 2.V2.8: Auto-hostlist на v2-сигналы (🟡)
  - Цель: `AutoHostlistService` принимает v2‑сигналы через `InspectionSignalsSnapshot` (без обязательной зависимости от legacy `BlockageSignals`).
  - Доп. критерии (после MVP, «полное вычищение legacy/bridge», ✅ выполнено):
    - [x] Удалена legacy-совместимость в `AutoHostlistService` (ветки/конструкторы/merge, завязанные на `BlockageSignals`).
    - [x] Оставлен только поток `InspectionSignalsSnapshot` (без legacy типов даже «для совместимости»).
    - [x] Добавлен smoke, подтверждающий работу auto-hostlist без legacy типов.
  - Gate:
    - Auto-hostlist продолжает фильтровать шум и не добавляет голые IP (домены только).

- [x] Задача 2.V2.9: UI — отключить legacy-рекомендации и legacy “справочно” (🟡)
  - Цель: после стабилизации v2 и SignalSequence-only убрать любые legacy рекомендации из карточек.
  - Gate:
    - Non-[V2] сообщения не меняют `BypassStrategy`.
    - В UI остаются только v2-диагноз + v2-план.
  - Доказательство (код):
    - `TestResultsManager.ParsePipelineMessage(...)` игнорирует строки рекомендаций без префикса `[V2]`.
    - `DiagnosticOrchestrator.TrackRecommendation(...)` игнорирует non-v2 строки и хранит только v2 рекомендации/план.

- [x] Задача 2.V2.10: Guard-тесты на отсутствие legacy в v2 пути (🟡)
  - Цель: чтобы `BlockageSignals`/`GetSignals(...)` не вернулись незаметно.
  - Gate:
    - Smoke `dpi2 --strict` + отдельный guard (grep/инварианты) проходит в CI/локально.

- [x] Задача 2.V2.11: QUIC fallback (DROP UDP/443) + метрика (🔴)
  - Цель: сделать TCP/TLS обход эффективным для целей, которые уходят в QUIC/HTTP3 (например браузерный YouTube).
  - Реализация: добавлен явный флаг `DropUdp443` (UI тумблер `QUIC→TCP`) + счётчик дропа в метриках.
  - Gate:
    - В метриках растёт счётчик UDP/443 drop при открытии цели.
    - При отключении bypass подавление прекращается.

- [x] Задача 2.V2.11.1: **Убрать "v2"-магию и сделать явные assist-флаги** (🔴 КРИТИЧЕСКИЙ)
  - **Проблема (было):** поведение QUIC fallback и allow-no-SNI было спрятано за строкой `PresetName == "v2"`.
    - v2 план выставлял обычные пресеты фрагментации ("Стандарт/…/План v2"), поэтому скрытый режим фактически не включался.
    - В итоге создавалось ощущение, что «по рекомендации включил — не работает».
  - **Решение (сделано):** введены явные флаги:
    - `DropUdp443` (UI тумблер `QUIC→TCP`)
    - `AllowNoSni` (UI тумблер `No SNI`)
    - Никакой зависимости от пресета фрагментации.
  - Gate:
    - Нигде в коде нет гейтинга по `PresetName == "v2"`.
    - Тумблеры и apply v2 используют один и тот же набор `TlsBypassOptions`.

- [x] Задача 2.V2.11.2: v2 рекомендации для assist-флагов (🟡)
  - Цель: чтобы v2 план мог предлагать `DropUdp443` и/или `AllowNoSni` как часть плана (assist-флаги), а не «магией».
  - Реализация (MVP):
    - `DropUdp443`: рекомендуется только вместе с TLS-обходом (TCP-only), если есть признаки QUIC/UDP активности (`UdpUnansweredHandshakes >= 2`).
    - `AllowNoSni`: рекомендуется только вместе с TLS-обходом, если в окне тестов доля “no SNI” высокая (минимум 2 теста и доля `HostTestedNoSniCount / HostTestedCount >= 0.70`).
  - Gate:
    - В v2 рекомендации появляются токены `DROP_UDP_443` / `ALLOW_NO_SNI`.
    - Ручной apply (`ApplyV2PlanAsync`) включает соответствующие тумблеры (`QUIC→TCP` / `No SNI`).

- [x] Задача 2.V2.12: Bypass State Manager (single source of truth) (🔴)
  - Цель: один владелец жизненного цикла WinDivert/TrafficEngine и активного плана; все apply/disable/rollback только через него.
  - Реализация:
    - `BypassStateManager` как SSoT для `TrafficEngine`/`TlsBypassService`.
    - Guard на прямые вызовы: в обычном режиме логирует ошибку, в smoke strict — выбрасывает исключение.
    - Smoke gate: `DPI2-026` (проверяет запрет действий «мимо менеджера» в strict).
  - Gate:
    - Невозможно поменять состояние bypass «мимо менеджера» (как минимум в smoke strict; иначе — ошибочный лог).
    - Smoke `dpi2 --strict` проходит с `DPI2-026`.

- [x] Задача 2.V2.13: Lite Watchdog + crash recovery (🔴)
  - Цель: fail-safe — после крэша/зависания движка обход гарантированно выключается.
  - Реализация:
    - Журнал сессии bypass (LocalAppData) с флагами `CleanShutdown`/`WasBypassActive`.
    - Crash recovery при старте: если прошлый shutdown был не clean при активном bypass → принудительный `Disable`.
    - Heartbeat watchdog: при активном bypass и отсутствии heartbeat/метрик дольше окна → авто-`Disable`.
  - Gate:
    - Smoke `dpi2 --strict` проходит с `DPI2-027`.

- [x] Задача 2.V2.14: Activation Detection (по метрикам) (🟡)
  - Цель: различать ENGINE_DEAD / NOT_ACTIVATED / ACTIVATED / NO_TRAFFIC / UNKNOWN без угадываний.
  - Реализация:
    - Детектор активации в `BypassStateManager` на основе последнего снимка метрик + таймстампов.
    - Статус выводится в UI-метриках как `ACT: ...`.
  - Gate:
    - Smoke `dpi2 --strict` проходит с `DPI2-028`.

- [x] Задача 2.V2.15: Outcome Check для HTTPS (tagged probe или UNKNOWN) (🟡)
  - Цель: честно сообщать SUCCESS/FAILED/UNKNOWN, не делая выводов по пассивным HTTP-кодам без MITM.
  - Реализация:
    - HTTPS outcome делается через tagged probe (активная TCP+TLS+HTTP проверка цели), при этом probe-трафик исключается из пользовательских метрик.
    - При отсутствии цели outcome остаётся UNKNOWN.
  - Gate:
    - Smoke `dpi2 --strict` проходит с `DPI2-029`.
    - Нет ложных «успех/провал» из пассивного анализа HTTPS.

- [x] Задача 2.V2.16: Смена сети — staged revalidation (без авто-применения) (🟡)
  - Цель: при смене сети предлагать пользователю «Проверить/Отключить/Игнорировать», а проверка делает health→activation→outcome.
  - Реализация:
    - `NetworkChangeMonitor` (debounce) подписывается на системные события смены сети и показывает уведомление в bypass-панели.
    - «Проверить» запускает staged revalidation:
      - Stage 1: `ActivationStatusSnapshot` (health/activation)
      - Stage 2: немедленный HTTPS outcome-probe (`RunOutcomeProbeNowAsync`)
      - Stage 3: текстовое предложение запустить полную диагностику (без auto-apply).
    - «Отключить» выключает bypass и отключает auto-bypass на следующий старт.
    - «Игнорировать» скрывает уведомление.
  - Gate:
    - Никакого auto-apply; только предложения и ручные действия.
    - Smoke `ui --strict` проходит с `UI-013`.

- [x] Задача 2.V2.17: Селективное подавление QUIC (UDP/443) по observed IP (🟡)
  - Цель: уменьшить побочные эффекты глобального UDP/443 drop, сохранив эффективность.
  - Реализация:
    - `BypassStateManager` держит observed IPv4 адреса цели по `host` (TTL=10 мин, cap=16), cold-start через DNS resolve.
    - `TlsBypassService` хранит snapshot этих IP и применяет его к `BypassFilter` при каждом Apply.
    - `BypassFilter` дропает UDP/443 только если dst ∈ observed IPv4 IP цели (для IPv6 пока остаётся прежнее поведение).
  - Gate:
    - Smoke `dpi2 --strict` проходит с `DPI2-030`.
    - UDP/443 к нецелевым IP не дропается.

- [x] Задача 2.V2.LAST: `DnsHijack` → стратегии (🟢)
  - Решение: при `DnsHijack` v2 рекомендует low-risk стратегию `UseDoh` (без TLS-обхода и без assist-флагов).
  - Gate:
    - Smoke `DPI2-034` проходит (DnsHijack → UseDoh only).


Примечание: архитектурный справочник и реализованные изменения см. в `ARCHITECTURE_CURRENT.md` и `CHANGELOG.md`.

---

## Фаза 4 — Рефакторинг и Технический долг (Q1 2026)
**Приоритет:** 🔵 Низкий
**Цель:** Улучшение поддерживаемости кода и архитектуры.

- Задача 4.0: Доминирование/веса планов v2 (🟡 техдолг после P0)
  - Цель: чтобы слабые сценарии не «откатывали» рекомендации, если уже нужен более сильный набор техник.
  - Подзадачи:
    - Ввести «доминирование» планов: если применён/активен план A, то планы B, где B ⊂ A, не рассматриваются как кандидаты.
    - Разделить ранжирование на 2 оси: эффективность (strength) и цена/инвазивность (cost).
    - Делать выбор и отсечение не только per-host, но и per-service/group (YouTube cluster).
    - Добавить feedback (успех/провал) для (group, plan) и учитывать при ранжировании.

- Задача 4.1: Внедрение DI Container
  - Переход на `Microsoft.Extensions.DependencyInjection`.
  - Уход от ручного создания зависимостей в `MainViewModel` и `App.xaml.cs`.
- Задача 4.2: Устранение глобального состояния
  - Рефакторинг `Config.ActiveProfile` (перевод в `IConfigService`).
  - Рефакторинг `Program.Targets` (перевод в `ITargetService`).
- Задача 4.3: Рефакторинг `DiagnosticOrchestrator`
  - Цель: уменьшить файл и разделить ответственность так, чтобы:
    - применение обхода/транзакции не жили внутри оркестратора;
    - ретест/переподключение (per-card) были отдельным модулем;
    - runtime‑состояние карточек и логика UI-обновлений были отделены от низкоуровневых сервисов.
  - Предлагаемая декомпозиция (минимально инвазивно, можно partial + отдельные классы):
    - Orchestrator.Pipeline (запуск/останов LiveTestingPipeline, прокидка progress, CT/таймауты, health-лог)
    - Orchestrator.Recommendations (парсинг/трек рекомендаций v2, маппинг в модели карточек)
    - Orchestrator.CardActions (вызовы per-card: «применение обхода», «ретест», «переподключение» — без реализации низкого уровня)
    - Orchestrator.State (in-memory состояние по карточкам: attachment, last transaction status, last retest status)
  - Жёсткие границы ответственности (важно):
    - Реальное применение обхода и сбор Snapshot транзакции: отдельный сервис (SSoT рядом с BypassStateManager), не Orchestrator.
    - Orchestrator только инициирует и отображает результат (TransactionResult/Snapshot) в UI.
  - План рефакторинга по шагам:
    - Шаг 1: Выделить модели состояния (CardState/Attachment/RetestState) в отдельный файл/папку.
    - Шаг 2: Вынести code paths «post‑apply ретест/переподключение» из Orchestrator в отдельный компонент CardActions.
    - Шаг 3: Упростить публичный API Orchestrator: оставить Start/Stop диагностики + select card + request actions.
    - Шаг 4: Обновить архитектурные документы после разделения (ARCHITECTURE_CURRENT.md и docs/full_repo_audit_v2.md).
- Задача 4.4: Рефакторинг `TestResultsManager`
  - Выделение логики парсинга сообщений в отдельный сервис.

---

## Фаза 5 — Native Core (Rust DLL) (Q2 2026)
**Приоритет:** 🔵 Низкий (после стабилизации P0)
**Цель:** Выделение performance-critical кода в Rust DLL для memory safety и ускорения hot path.

### Анализ кандидатов

| Модуль | Rust выгода | Сложность | Рекомендация |
|--------|-------------|-----------|--------------|
| **WinDivertNative** | ✅✅✅ | Low | Rust DLL — уже P/Invoke, полностью изолирован |
| **PacketParser** (IP/TCP/UDP) | ✅✅✅ | Low-Medium | Rust DLL — hot path, zero-copy parsing |
| **TLS/SNI Parser** | ✅✅ | Medium | В общую Rust DLL с PacketParser |
| **BypassFilter** | ✅ | High | .NET DLL — callbacks через FFI слишком сложны |
| **DNS Parser** | ⚠️ | Medium | Оставить — малая выгода относительно усилий |

### Целевая архитектура

```
ISP_Audit.exe (.NET)
    │
    ├── BypassController / UI / DiagnosticOrchestrator
    │
    └── P/Invoke
            │
            ▼
isp_audit_native.dll (Rust)  ← WinDivert FFI, PacketParser, TLS Parser
            │
            ▼
WinDivert.dll (native driver)
```

### Задачи

- [ ] Задача 5.0: Подготовка инфраструктуры Rust (🟢)
  - Подзадачи:
    - 5.0.1: Создать Rust workspace `native/isp_audit_native/`
    - 5.0.2: Настроить `Cargo.toml` с `crate-type = ["cdylib"]`
    - 5.0.3: Настроить MSBuild для автоматической сборки Rust DLL при `dotnet build`
    - 5.0.4: Добавить копирование `isp_audit_native.dll` в output directory
  - Gate: `dotnet build` собирает и копирует Rust DLL без ручных шагов

- [ ] Задача 5.1: WinDivert FFI обёртка на Rust (🟡)
  - Цель: заменить `WinDivertNative.cs` на безопасную Rust обёртку с C ABI.
  - Подзадачи:
    - 5.1.1: Реализовать структуры `DivertAddress`, `DivertIpHdr`, `DivertTcpHdr` в Rust
    - 5.1.2: Реализовать `divert_open`, `divert_recv`, `divert_send`, `divert_close`
    - 5.1.3: Реализовать `divert_calc_checksums` с валидацией буфера
    - 5.1.4: Создать C# interop класс `WinDivertNativeRust.cs`
    - 5.1.5: Feature flag для переключения между старым и новым P/Invoke
  - Gate: NATIVE-001 — smoke-тест WinDivert через Rust DLL проходит (требует admin)

- [ ] Задача 5.2: Packet Parser на Rust (🟡)
  - Цель: zero-copy парсинг IP/TCP/UDP заголовков с bounds checking.
  - Подзадачи:
    - 5.2.1: Реализовать `parse_ip_header(buffer, len) -> IpHeaderInfo`
    - 5.2.2: Реализовать `parse_tcp_header(buffer, len) -> TcpHeaderInfo`
    - 5.2.3: Реализовать `is_tcp_rst(buffer, len) -> bool`
    - 5.2.4: Реализовать `get_payload_offset(buffer, len) -> u32`
    - 5.2.5: Интегрировать в `BypassFilter` через P/Invoke
  - Gate: NATIVE-002 — бенчмарк показывает ≥20% ускорение на packet parsing

- [ ] Задача 5.3: TLS/SNI Parser на Rust (🟢)
  - Цель: безопасный парсинг TLS ClientHello с извлечением SNI.
  - Подзадачи:
    - 5.3.1: Реализовать `is_client_hello(payload, len) -> bool`
    - 5.3.2: Реализовать `extract_sni(payload, len, out_buffer, out_len) -> bool`
    - 5.3.3: Объединить дублированный код из `TlsBypassService` и `BypassFilter`
    - 5.3.4: Интегрировать в `BypassFilter` и `TrafficCollector`
  - Gate: NATIVE-003 — SNI extraction корректен на тестовом наборе ClientHello

- [ ] Задача 5.4: Опционально — .NET Bypass DLL (🟢)
  - Цель: выделить Bypass-логику в отдельную сборку `ISP_Audit.Bypass.dll`.
  - Подзадачи:
    - 5.4.1: Создать проект `ISP_Audit.Bypass.csproj`
    - 5.4.2: Перенести `BypassFilter`, `TlsBypassService`, `BypassStateManager`
    - 5.4.3: Определить публичный API через интерфейсы
    - 5.4.4: Обновить зависимости в основном проекте
  - Gate: Bypass-функциональность работает через отдельную DLL без регрессий

### Блокеры и риски
- **CI/CD**: тестирование Rust DLL требует Windows machine с WinDivert driver и правами администратора
- **Бинарная совместимость**: структуры `DivertAddress`, `DivertIpHdr` должны быть layout-совместимы
- **Производительность**: FFI overhead может нивелировать выгоду — нужны бенчмарки на реальном трафике

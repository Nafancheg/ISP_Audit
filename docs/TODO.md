# ISP_Audit — TODO (только невыполненное)

Дата: 05.02.2026 (актуализировано)
Назначение: список незакрытых задач и проблем. Всё выполненное и историческое — в CHANGELOG.md и ARCHITECTURE_CURRENT.md.

---

## Приоритеты
- 🔴 Критический: влияет на корректность детекции/обхода или стабильность рантайма
- 🟡 Важный: повышает точность/надёжность, снижает риск регрессий
- 🟢 Низкий: UX/полиш/интеграции

---

## Примечания по docs/plan3.md

- План структурный и исполняемый: есть зависимость P0.1 → блокирует остальное, и есть чёткие deliverables/критерии успеха.
- Часть roadmap по Policy-Driven в проекте уже реализована (см. CHANGELOG.md); в TODO оставлены только незакрытые хвосты и “следующий уровень” (лимиты/перф/advanced UI).
- Фокус для ближайших итераций: P0.1 (краш цикла) + P0.2 (прозрачность/таймауты Apply) → затем P1 (понятность UI/группы/видимость QUIC→TCP).

## Последние проверки
- [x] 27.01.2026: smoke ui (non-admin) PASS 13/13
- [x] 28.01.2026: dotnet build OK
- [x] 29.01.2026: smoke reg (non-admin) PASS 19/19
- [x] 30.01.2026: smoke strict (admin) PASS 153/153, отчёт: [artifacts/smoke_strict_20260130_162331.json](artifacts/smoke_strict_20260130_162331.json)
- [x] 02.02.2026: dotnet build OK
- [x] 02.02.2026: smoke reg (non-admin) PASS 19/19
- [x] 02.02.2026: smoke ui (non-admin) PASS 13/13
- [x] 02.02.2026: smoke strict (admin) PASS 153/153, отчёт: [artifacts/smoke_strict_20260202_114445.json](artifacts/smoke_strict_20260202_114445.json)
- [x] 02.02.2026: dotnet build OK (auto-apply/SNI fixes)
- [x] 02.02.2026: smoke reg (non-admin) PASS 19/19 (auto-apply/SNI fixes)
- [x] 02.02.2026: smoke ui (non-admin) PASS 13/13 (auto-apply/SNI fixes)
- [x] 02.02.2026: smoke reg (non-admin) PASS 20/20 (post-apply persistence)
- [x] 02.02.2026: smoke ui (non-admin) PASS 13/13 (post-apply persistence)
- [x] 02.02.2026: smoke ui (non-admin) PASS 14/14 (P1.8 primary status)
- [x] 02.02.2026: smoke strict (admin) PASS 155/155, отчёт: [artifacts/smoke_strict_20260202_173725.json](artifacts/smoke_strict_20260202_173725.json)
- [x] 03.02.2026: dotnet build OK
- [x] 03.02.2026: smoke ui (non-admin) PASS 14/14 (P1.9 aggregation wiring)
- [x] 03.02.2026: smoke reg (non-admin) PASS 21/21 (REG-021 hard disable)
- [x] 04.02.2026: dotnet build OK
- [x] 04.02.2026: smoke ui (non-admin) PASS 15/15
- [x] 04.02.2026: dotnet build OK (Operator UI compact header)
- [x] 04.02.2026: smoke ui (non-admin) PASS 15/15 (Operator UI compact header)
- [x] 04.02.2026: dotnet build OK (строгое подтверждение разрешения DNS/DoH)
- [x] 04.02.2026: smoke reg (non-admin) PASS 21/21 (consent gate + confirm)
- [x] 04.02.2026: smoke ui (non-admin) PASS 17/17 (UI-017 consent toggle guard)
- [x] 04.02.2026: dotnet build OK (DEBUG-only харднинг ISP_AUDIT_TEST_SKIP_TLS_APPLY)
- [x] 04.02.2026: smoke reg (non-admin) PASS 21/21 (DEBUG-only харднинг ISP_AUDIT_TEST_SKIP_TLS_APPLY)
- [x] 04.02.2026: smoke ui (non-admin) PASS 17/17 (DEBUG-only харднинг ISP_AUDIT_TEST_SKIP_TLS_APPLY)
- [x] 05.02.2026: smoke strict (admin) PASS 159/159, отчёт: [artifacts/smoke_strict_20260205_103623.json](artifacts/smoke_strict_20260205_103623.json)
- [x] 05.02.2026: dotnet build -c Release OK
- [x] 05.02.2026: dotnet publish -c Release OK
- [x] 05.02.2026: Документация: добавлен реестр переменных окружения `docs/ENV_VARS.md`
- [x] 05.02.2026: smoke reg (non-admin) PASS 22/22 (REG-023: env var registry guard)
- [x] 05.02.2026: smoke ui (non-admin) PASS 17/17
- [x] 05.02.2026: Refactor: централизовано чтение/парсинг ENV в `Utils/EnvVar.cs` (dotnet build + smoke reg/ui OK)
- [x] 05.02.2026: Refactor: тайминги `ISP_AUDIT_*_MS` (watchdog/activation/outcome) читаются через `EnvVar` (dotnet build + smoke reg/ui OK)
- [x] 05.02.2026: Refactor: Program использует `EnvVar` для gate-check `ISP_AUDIT_POLICY_DRIVEN_TCP443` (dotnet build + smoke reg/ui OK)

- [x] 06.02.2026: dotnet build OK (EnvKeys)
- [x] 06.02.2026: smoke reg (non-admin) PASS 22/22 (EnvKeys)
- [x] 06.02.2026: smoke ui (non-admin) PASS 17/17 (EnvKeys)
- [x] 06.02.2026: Refactor: централизованы строковые ключи ENV в `Utils/EnvKeys.cs` (dotnet build + smoke reg/ui OK)

---

## P0 — Стабилизация обхода и рантайма (КРИТИЧЕСКОЕ)

- 🔴 P0.0: Стабилизация браузерного кейса (YouTube/Google)
  - Цель: YouTube видео стабильно стартует в браузере при включённом обходе (админ, WinDivert активен).
  - Definition of done:
    - Видео стартует без ручного «тыкания» тумблеров (максимум 1–2 осмысленных действия: apply + переподключение).
    - Есть наблюдаемость активного воздействия (например рост счётчиков дропа UDP/443 при QUIC→TCP).
    - После закрытия приложения DNS/DoH возвращаются в исходное состояние.
  - Ближайшие шаги:
    - Зафиксировать 1–2 эталонных сценария воспроизведения (браузер/провайдер/профиль, QUIC включён/выключен).
    - Регресс‑bisect: найти период, когда «работало лучше», и сравнить отличия в Bypass/TrafficEngine/DNS.
    - Добавить/оформить режим «classic/минимальный набор» (фиксированный набор техник, минимум автоматики) для baseline.
    - [x] UX: добавить режим "Фокус" для таблицы результатов (показывать проблемные/активные/применённые строки) + компактную сводку "что применено" в шапке результатов.
    - [x] FixService: при восстановлении DNS удалять добавленные DoH-профили/правила (PowerShell/netsh) и возвращать исходный `EnableAutoDoh` (если был сохранён снимок)
    - [x] Устранить рассинхрон целей в UI для рекомендаций INTEL: строки плана несут контекст `host=... SNI=... RDNS=...`, а UI привязывает стратегию к карточке по `host=` (через `SelectUiKey`), не по `LastUpdatedHost`.
    - [x] Fix: отложенные per-card ретесты не запускаются после отмены; при режиме "без лимита времени" ретест не ставится в очередь "после завершения".
    - [x] Fix: транзакционность Start/Cancel — ранний Cancel (до создания CTS) не теряется, отмена применяется сразу после инициализации.
    - [x] Fix: нормализация dedupKey для рекомендаций INTEL (санитизация + strip `www.`) и case-insensitive кэш эмита, чтобы одинаковые рекомендации не спамились для `youtube.com`/`www.youtube.com` и вариаций регистра.
    - [x] UX: публиковать планы и печатать рекомендации (INTEL) даже при `DNS/TCP/TLS ✓` (LogOnly), чтобы «OK, но есть вмешательство» было видно без красных карточек.
    - [x] Де-версионирование рекомендаций: в коде/тестах/доках убраны версионные маркеры; используется единый формат INTEL (`[INTEL]`, `intel:`, `plan:`).
    - [x] Perf: ускорение live-диагностики — `LiveTestingPipeline.TesterWorker` выполняет тесты параллельно с лимитом `PipelineConfig.MaxConcurrentTests` (SemaphoreSlim/in-flight), чтобы очередь меньше раздувалась при активном браузере.
    - [x] Убрать «автоматику» для assist в INTEL-плане: токены `DROP_UDP_443/ALLOW_NO_SNI` остаются в рекомендациях, но по умолчанию не применяются автоматически (требуется явное включение пользователем).
    - [x] INTEL: QUIC→TCP (DROP_UDP_443) не рекомендовать, если уже есть `TLS_HANDSHAKE_TIMEOUT` на TCP/443 (приоритет — лечение TLS, чтобы не создавать ложный «приоритет UDP»).
    - [x] INTEL/Tester: добавить реальный probe HTTP/3 (QUIC) и протащить в сигналы/диагноз (QuicInterference) + вывод в UI (H3:✓/✗/-) и приоритизация DropUdp443 по факту H3.
    - [x] Regression smoke: `REG-017` — H3 fail-only → QuicInterference и assist DropUdp443; при TLS timeout DropUdp443 не рекомендуется.
    - [x] INTEL: HttpRedirect → план обхода (минимум: `HttpHostTricks`). Regression smoke: `REG-018`.
    - [x] INTEL: справочник blockpage-hosts вынесен во внешний JSON `state\\blockpage_hosts.json` (configurable). Regression smoke: `REG-019`.
    - [x] INTEL: feedback store подключён в рантайме (persist: `state\\feedback_store.json`), результат записывается после Post-Apply ретеста (✓/❌ по target IP, при наличии SNI — приоритетно по SNI-match). Если по цели одновременно есть ✓ и ❌ → outcome считается неоднозначным и в store не пишется. При `DropUdp443` outcome также записывается для `StrategyId.QuicObfuscation`.
    - [x] Отключить AutoRetest по изменению тумблеров bypass по умолчанию (для чистой наблюдаемости).
    - [x] P0.x Реактивная синхронизация targets для селективного QUIC→TCP: refresh UDP/443 targets по UDP blockage (без глобального DropUdp443)
      - [x] Assist-флаги (QUIC→TCP, No-SNI) применяются при ручном Apply
      - [x] DoH/DNS из плана применяется только при явном согласии пользователя (consent gate, state\\operator_consent.json)
      - [x] Логика синхронизации вынесена в слой Runtime Adaptation: `ReactiveTargetSyncService`
      - [x] Bounded queue + coalescing + retry-until-delivered (TTL/attempts) для защиты от stop/start и burst событий

- 🔴 P0.1: TrafficEngine crash: `Collection was modified; enumeration operation may not execute`
  - Симптом: в логе появляется `Loop crashed: Collection was modified; enumeration operation may not execute.`
  - Риск: часть функциональности может “молча” отключиться (цикл упал), а UI продолжит выглядеть живым.
  - Чек‑лист (из docs/plan3.md, адаптация):
    - [ ] Собрать контекст: ±100 строк лога вокруг краша, определить компонент/подсистему.
    - [x] Улучшить лог краша: печатать `ex.ToString()` (а не только `ex.Message`) в `Core/Traffic/TrafficEngine.cs` (в месте `Loop crashed: ...`).
    - [x] Добавить stack trace для всех исключений + thread id + контекст выполнения.
    - [x] Добавить correlation id для Apply/Retest/PostApplyRetest и привязать Apply к crash-логам `TrafficEngine` (через `BypassOperationContext` + `TrafficEngine.SetLastMutationContext`).
    - [x] Проставлять `lastMutation` также при регистрации/удалении фильтров через `BypassStateManager.RegisterEngineFilter/RemoveEngineFilter` (например reconnect-nudge), чтобы ловить контекст мутаций вне Apply.
    - [x] Фикс: `TrafficEngine` итерируется по snapshot массива фильтров (обновляется на Register/Remove/Clear), чтобы реэнтрантные мутации списка фильтров во время `filter.Process(...)` не могли вызвать `Collection was modified`.
    - [x] Regression smoke: `INFRA-006` — реэнтрантный фильтр (Register/Remove во время обработки) не должен ронять обработку пакета.
    - [x] Regression smoke: `INFRA-007` — конкурентный churn фильтров (параллельные Register/Remove) + обработка пакетов не должны падать.
    - [x] Stress smoke: `INFRA-008` — rapid Apply/Disable (через `BypassStateManager`) во время активной обработки пакетов не должен приводить к падениям/зависаниям.
    - [x] Stress smoke: `INFRA-009` — конкурентные обновления decision snapshot + UDP targets во время обработки пакетов не должны приводить к падениям.
    - [x] Составить карту «подозреваемых коллекций» и всех чтений/записей: [docs/P0_1_suspect_collections.md](docs/P0_1_suspect_collections.md)
    - [x] Strict smoke all (через `SmokeLauncher` + UAC): PASS 149/149 (FAIL=0, SKIP=0), отчёт: [artifacts/smoke_strict_20260121_122319.json](artifacts/smoke_strict_20260121_122319.json)
    - [x] Crash report `TrafficEngine`: при падении loop сохраняется best-effort JSON рядом с приложением: `state\\crash_reports\\traffic_engine\\` (исключение + lastMutation).
    - [ ] Описать ручной сценарий воспроизведения (точная последовательность действий).
    - [ ] Сделать воспроизводимость ≥80% (цель: краш за ~2 минуты).
    - [ ] Добавить regression сценарий (integration/stress): rapid apply/переключение во время активной диагностики.
    - [ ] Зафиксировать baseline hot path (latency p50/p95/p99, alloc rate, CPU) перед фиксом.
    - [x] Аудит/харднинг probe-cleanup: `_probeFlowsUntilTick` в `Core/Traffic/Filters/BypassFilter.ProbeFlows.cs` хранится в `ConcurrentDictionary` (перечисление snapshot-safe), добавлены проверки и защита от лишнего шума в логах.
    - [x] Hotfix: подписчики/единичные исключения не должны валить `TrafficEngine.Loop` (guard + per-packet catch).
    - [x] Сделать входные списки профиля фактически immutable: хранить `TlsFragmentSizes/RedirectRules/Hosts` как массивы, чтобы исключить `Collection was modified` при перечислении в hot path.
    - [ ] Добавить unit/регресс тесты на concurrent read/write и «модификация во время iteration».
    - [ ] Stress: ≥1000 Apply/Rollback за минуту + проверка утечек и стабильности.
    - [ ] Проверить перф: если деградация >10% → вариант с lock; если >30% → откат и переход на immutable snapshot/Concurrent*.
    - [ ] Временный workaround: debounce/ограничение слишком частых Apply во время активной диагностики + WARN в UI/логе.

- 🟡 P0.2: Apply timeout — детализация и диагностика причин
  - Цель: при таймауте/зависании видеть точную фазу и причину; Apply завершается <3с в 95% случаев.
  - Заметка (03.02.2026): найден и исправлен сценарий "Disable через Apply" → возможное "воскрешение" bypass из remembered per-target union при Shutdown/Disable; закрепляем регрессионным smoke, чтобы не вернуть проблему.
  - Чек‑лист:
    - [x] Добавить фазовую диагностику ApplyIntelPlan (фазы + длительности + текущая фаза + общая длительность) и протащить её до persist Apply-транзакций (Result).
    - [x] Regression smoke: `REG-016` — детерминированный Apply-timeout (через `ISP_AUDIT_TEST_APPLY_DELAY_MS`) должен содержать фазовую диагностику.
    - [x] Добавить фазовое логирование Apply в `Bypass/TlsBypassService.Engine.cs` (Preparing config → DNS resolve → Stop old filters → Start new filters → Validation) с duration.
    - [x] Обернуть apply на уровне `Bypass/BypassStateManager.cs`: единая точка логирования (Started/PhaseCompleted/Completed/Rollback) + correlation id.
    - [x] Добавить пороги на фазы (WARN/OK) + structured events (без доп. внутренних таймаутов на фазах).
    - [x] Добавить диагностику длительностей `TrafficEngine.StartAsync/StopAsync` (WARN при slow path) для расследования зависаний WinDivert.
    - [x] Fix: `FixService` поддерживает отмену/timeout для внешних команд (netsh/powershell), токен Apply прокинут в DNS/DoH операции (таймаут больше не «залипает» вне cancellation).
    - [x] Fix: остановка `TrafficEngine` при отсутствии потребителей выполняется через `BypassStateManager` (убирает "в обход BypassStateManager" и снижает риск watchdog/ENGINE_DEAD).
    - [x] Fix: `DisableAll` выключает bypass через `BypassStateManager.DisableTlsAsync` и чистит remembered active targets (не даём bypass «воскреснуть» из per-target union при Shutdown/Disable).
    - [x] Fix: watchdog/activation `ENGINE_DEAD` считают grace от момента, когда движок реально стал не запущен (а не от старого времени Apply), чтобы избежать ложных срабатываний.
    - [x] Regression smoke: `REG-021` — "hard disable" (`DisableTlsAsync`/`DisableAll`) очищает per-target union и не приводит к повторному `engine_start`/effective ON на shutdown.
    - [ ] Собрать логи с детализацией на реальном таймауте/зависании.
    - [ ] Классифицировать причину (WinDivert stop, DNS, deadlock, connectivity check).
    - [ ] Проверить потенциальные зависания в `Core/Traffic/TrafficEngine.cs` (например `StopAsync`/`StartAsync`) и добавить диагностику/таймауты при необходимости.
    - [ ] Реализовать фикс по причине и проверить KPI: Apply <3с в 95% случаев.

- 🟡 P0.3: Глобальное логирование критических ошибок (полировка)
  - Чек‑лист:
    - [ ] Аудит всех try/catch: stack trace + контекст состояния.
    - [x] Global exception handler: необработанные исключения → crash report (best-effort JSON в `state\\crash_reports\\app\\`).
    - [x] Подключить `TaskScheduler.UnobservedTaskException` (точка: `App`) и сохранять `ex.ToString()` в crash report.

- 🟡 P0.4: Харднинг тестовых хуков и Release-проверки
  - Чек‑лист:
    - [x] Прогнать `smoke strict (admin)` через `SmokeLauncher` и сохранить отчёт в `artifacts/`.
    - [x] Выполнить `dotnet build -c Release` и `dotnet publish -c Release` (single-file), проверить что сборка проходит.
    - [x] Убедиться, что тестовые хуки `ISP_AUDIT_TEST_*` не меняют поведение Release-сборки (как минимум: `ISP_AUDIT_TEST_SKIP_TLS_APPLY` должен быть DEBUG-only).
    - [x] Аудит всех `ISP_AUDIT_TEST_*` в коде: классифицировать и ограничить DEBUG-only, либо оформить явный контракт/док.

---

## P1 — Высокий приоритет

- 🟡 P1.1: Deduplicate Apply для одного домена
  - Чек‑лист:
    - [ ] Не выполнять повторное применение, если effective config не изменился (сообщение “уже применено”).
    - [ ] Защититься от “apply storm” при кликах по одинаковым целям.

- 🟡 P1.2: Группировка связанных доменов в UI
  - Чек‑лист:
    - [x] Кросс-доменная группировка (не только по суффиксу), например YouTube: `youtube.com` + `googlevideo.com`.
      - Реализация: pinned-группы в `state/domain_groups.json`, агрегация карточек в один groupKey + кнопка "Подключить (группа: …)".
      - Apply: оркестратор применяет план из любого домена группы к anchor-домену (OutcomeTargetHost=anchor) + union endpoint snapshot.
      - Smoke: `CFG-007`.
    - [x] Авто-определение связанных доменов (learned groups, suggest-only) по co-occurrence в UI-сессии.
      - Персист: `state/domain_groups.json` → `LearnedGroups`.
      - Ограничения: обучаем только домены (не IP), игнорируем шумовые, pinned имеет приоритет.
      - Smoke: `CFG-008`.
    - [x] UX для learned-групп: скрыть подсказку (ignore) и «закрепить» learned → pinned.
      - Персист: `state/domain_groups.json` → `IgnoredLearnedGroupKeys` + перенос в `PinnedGroups`.
      - Smoke: `CFG-009` (ignore) / `CFG-010` (promote).

- 🟡 P1.3: Видимость селективного UDP/443 (QUIC→TCP)
  - Чек‑лист:
    - [ ] В UI показать, что UDP/443 реально дропается “по целевым IP”, а не “где-то в системе”.
    - [ ] Вывести счётчики/сводку по target/group (сколько дропов и по каким endpoint-ам).
    - [ ] Использовать существующий API `TlsBypassService.GetUdp443DropTargetIpsSnapshot()` и метрику `_udp443Dropped` из `Core/Traffic/Filters/BypassFilter.Metrics.cs`.

- 🟡 P1.4: Post-crash диагнозы
  - Чек‑лист:
    - [ ] После восстановления показать пользователю, что произошло (какой модуль упал, что отключилось, что делать дальше).

- 🟡 P1.5: Строгая транзакционность Start/Cancel/Retest/Apply (сериализация операций)
  - Проблема: даже при pending-cancel остаются теоретические гонки при быстрых кликах Start/Cancel/Retest/Apply.
  - Цель: гарантировать отсутствие пересечений фаз и «двух активных операций» на одном оркестраторе.
  - Чек‑лист:
    - [ ] Ввести единый async-lock/очередь операций для Orchestrator (Start/Cancel/Retest/PostApplyRetest/ApplyIntelPlan).
    - [ ] Сделать явные состояния/переходы (state machine) и логировать транзакции (Started/Cancelled/Completed + причина).
    - [ ] Добавить smoke/reg сценарий: rapid Start→Cancel→Start→Retest без неожиданных ретестов/повисаний.

- 🟡 P1.6: Ускорение live-диагностики — приоритизация и деградация очередей
  - Контекст: после включения параллельного тестера остаётся риск «утопить» важные цели в фоне при активном браузере (много коннектов/шумных доменов).
  - Цель: быстрее получать первые 1–3 осмысленные проблемы и снижать рост `pending` без потери важных целей.
  - Чек‑лист:
    - [ ] Разделить входную очередь на high/low priority (например, два `Channel<HostDiscovered>`):
      - high: manual retest/ForceRetest, цели из профиля/critical, хосты с признаками блокировки/повторными фейлами.
      - low: фоновые/шумовые/успешные кандидаты.
    - [ ] Политика дропа: при переполнении low-очереди — дропать агрессивнее (или укорачивать тест), high не дропать.
    - [ ] «Degrade mode» по health-сигналам: если `PendingCount` выше порога или растёт несколько тиков подряд — временно:
      - снижать timeout для low-priority тестов,
      - ужесточать гейтинг/кулдауны для low,
      - ограничивать объём логов/статусов для low.
    - [ ] Метрики задержек: логировать возраст элемента (enqueue→start test→classified), чтобы видеть хвосты и эффект приоритизации.
    - [ ] Smoke/perf: добавить сценарий перегруза (высокий enq rate) и проверить, что high-цели стабильно проходят за bounded время.

- 🔴 P1.7: Post-Apply Retest — гарантировать перепроверку после Apply
  - Проблема: обход мог сработать (видео/сайт оживает), но UI продолжает показывать старые ошибки, если пост-проверка не запустилась/не отразилась.
  - Цель: любой Apply (ручной и авто) приводит к явному событию «перепроверено» и обновлению статуса в UI.
  - Чек‑лист:
    - [x] Если диагностика сейчас идёт — не “skip and forget”: добавить цели ретеста в очередь активного `LiveTestingPipeline` (+ best-effort OUT probe).
    - [x] Явно логировать Post-Apply Retest: старт/финиш/причины/режим (enqueue/local) + correlation id.
    - [x] Пост-Apply Retest обновляет статус карточек/группы (badge): `Пост‑проверка: …` → `OK/FAIL/частично/нет данных`.
    - [x] Персист: сохранять в state результат последней пост-проверки по groupKey (время + итог + краткие детали) → `state/post_apply_checks.json`.

- 🟡 P1.8: Семантика «работает» и отражение в UI (исправлено/частично/не проверено)
  - Цель: показать пользователю, что реально “починилось” после обхода, не теряя историю прошлых фейлов.
  - Чек‑лист:
    - [x] Критерий успеха (текущий): строгий — основной сигнал только из Post-Apply Retest (OK/FAIL/PARTIAL/UNKNOWN); мягкий сигнал оставлен как потенциальное улучшение.
    - [x] В UI добавлен отдельный слой семантики для пост-проверки (не смешивается с `TestStatus`): badge `Пост‑проверка: …`.
    - [x] В UI "текущий итог" стал основной меткой: `TestResult.PrimaryStatus/PrimaryStatusText` отражают пост‑проверку, при этом `Status/TestStatus` остаются неизменными.

- 🟡 P1.9: Агрегация CDN-подхостов в результатах (на базе groupKey)
  - Проблема: десятки `rr*`/CDN-подхостов засоряют таблицу, хотя это один реальный кейс блокировки.
  - Цель: отображать одну строку по группе (например `googlevideo.com`), с возможностью раскрыть детали по подхостам.
  - Чек‑лист:
    - [x] Использовать pinning `hostKey → groupKey` (state/group_participation.json) как источник groupKey для UI-ключа строки.
    - [x] Поддержать fallback на pinned/learned группы (state/domain_groups.json), если pinning ещё не задан.
    - [x] В агрегированной строке показывать бейдж количества членов (×N) и выставлять стабильный `UiKey` для конвертеров Group/ACTIVE.
    - [x] Intel-рекомендации привязывать к карточке также по `UiKey`, чтобы стратегия применялась к агрегированной строке.
    - [ ] Детали по подхостам — по раскрытию/в отдельной панели, без спама в основной таблице.

- 🟡 P1.10: Дедупликация истории Apply (UI/персист)
  - Проблема: повторы apply с тем же планом для одной группы создают шум и мешают понять “что реально применено”.
  - Цель: история должна быть читаемой: один актуальный Apply + счетчик повторов и детали по раскрытию.
  - Чек‑лист:
    - [x] UI: группировать/сворачивать apply-транзакции по ключу `(GroupKey + PlanText)` и показывать последний + `×N`.
    - [x] Детали: сохранять доступ к полному списку (для аудита), но по умолчанию показывать агрегированно (список в JSON выбранной строки).

- 🔴 P1.11: Второй UI — «Операторский Dashboard» + автопилот (минимум шума)
  - Проблема: текущий UI перегружен инженерными деталями (логи/пакеты/тумблеры). Конечный пользователь не принимает сетевых решений, он ожидает 1–2 действия и результат.
  - Цель: добавить второй пользовательский интерфейс «Operator» (антивирус‑метафора: щит/светофор) с автоприменением безопасных стратегий и понятным откатом. Инженерный интерфейс («Engineer») остаётся для диагностики/отладки.
  - Принципы:
    - Status‑first: один главный статус (OK/Анализ/Блокировка/Исправляем/Не удалось).
    - Контекстность: детали скрыты по умолчанию, показываем только события/итоги; сырые логи — только в Engineer.
    - Автопилот: пользователь не выбирает DoH/TLS/QUIC; он выбирает цель (сервис) и, при необходимости, даёт одно согласие на системные изменения.
    - Прогрессивное раскрытие: «простое сразу, сложное по запросу» (Operator чистый, Engineer для глубины).
    - Один CTA: на каждом шаге ровно одно главное действие (запустить / исправить / повторить / откатить).
    - Понятный язык: никаких TLS_AUTH_FAILURE/TCP_RESET на главном экране — только человеческие формулировки + краткий совет.
    - Семантика шкалы: «всё работает / работает, но не идеально / не работает» должна быть очевидна без тех. знаний.
  - Definition of done:
    - Приложение по умолчанию запускается в Operator UI (с возможностью открыть Engineer UI в 1 клик).
    - Operator UI умеет: старт/стоп мониторинга, «Исправить», «Откатить», показывает историю сессий (полный цикл: проверка → проблемы → действия → итог).
    - Автопилот применяет только безопасные/селективные действия без вопросов; системные изменения (DNS/DoH, глобальные воздействия) требуют явного согласия.
    - Есть наблюдаемость: что именно сейчас включено (на уровне понятных пользователю “эффектов”), без пакетов/hex.
    - Smoke/reg: новый режим не ломает существующие smoke (ui/reg/strict).
  - Чек‑лист (план работ):
    - [x] UX‑контракт Operator UI (1 страница в docs): статусы, тексты, состояния кнопок, события в «Истории активности» (см. `docs/P1_11_operator_ui_contract.md`).
    - [x] Таблица операторских состояний (иконка/цвет/текст/действие) как единый источник для UI (OperatorViewModel.Status):
      - [ ] Ожидание (серый): «Нажмите для проверки» → CTA «Проверить сеть».
      - [ ] Проверка (синий): «Анализ сети…» → CTA «Остановить».
      - [ ] Норма (зелёный): «Сеть работает нормально» → CTA «Проверить снова».
      - [ ] Ограничения (оранжевый): «Некоторые сервисы нестабильны» → CTA «Исправить».
      - [ ] Блокировка (красный): «Доступ заблокирован» → CTA «Исправить».
    - [ ] Поток (wizard) для Operator UI:
      - [ ] Шаг 1: выбор цели (сервис/игра) + одна большая кнопка «Проверить/Начать».
      - [x] Базовый выбор источника трафика в Operator: «Быстрая проверка интернета» или «Проверка выбранного приложения (.exe)» (как в Engineer).
      - [x] Шаг 2: прогресс диагностики (без таблиц/настроек): индикатор + «проверено X из Y».
      - [x] Шаг 3: итог (сводка): счётчики OK/нестабильно/блокируется + CTA.
      - [x] Уплотнение Operator UI: выбор источника сворачиваемый, поле .exe скрыто в режиме быстрой проверки, без дублирующих блоков.
      - [x] Подробности в Operator: человеко‑понятная сводка; сырая диагностика спрятана под вложенным Expander.
      - [x] Убраны дублирующие заголовки: одна компактная шапка + единый Expander «Подробнее».
      - [x] Шаг 4: «Исправляем…» + автопост‑проверка + итог «стало лучше/частично/не помогло/не удалось проверить».
    - [ ] Выбор игры/сервиса без ручного пути:
      - [ ] Список “последние/популярные” (плитки/иконки) + второстепенная кнопка «Обзор…».
      - [ ] Минимум галочек на первом экране (остальное под «Доп. параметры»).
    - [ ] Навигация/режимы:
      - [x] Operator UI как отдельное окно/вью (новый XAML), Engineer UI = текущий MainWindow.
      - [ ] Переключение режимов: кнопка/иконка «Расширенный режим», опционально CLI `--advanced`.
      - [ ] Подтверждение перехода в Engineer UI: «Вы переходите в расширенный режим — здесь доступны технические детали и настройки. Продолжить?»
      - [x] Возврат из Engineer в Operator (кнопка в MainWindow, без shutdown shared VM).
      - [x] Персист выбранного режима в `state\\ui_mode.json` (или аналог), чтобы помнить выбор.
      - [x] Shared `MainViewModel`: создавать единый экземпляр в `App` и назначать в оба окна.
      - [ ] Минималистичный header (лево: название, право: иконки ⚙️ и ?):
        - [ ] ⚙️ открывает модальное окно «Настройки» (Operator-safe параметры, без инженерных тумблеров).
        - [ ] ? открывает краткую справку «как пользоваться» + ссылка «Открыть расширенный режим».
    - [ ] ViewModel/состояние:
      - [x] Новый `OperatorViewModel` (или аналог) без технических тумблеров.
      - [ ] Маппинг существующей семантики (`PrimaryStatus`, пост‑проверка, active/applied) в 5–6 операторских состояний.
      - [x] Отдельная модель «событий» (не пакетов): время + сервис/группа + тип вмешательства + действие + результат.
      - [x] История в Operator как «список сессий» (вместо event-spam):
        - Каждая сессия = один полный цикл: что тестировали → список проблем → что применили → результат post‑apply retest (если был).
        - UI: карточки-сессии (сводка) + раскрытие деталей (внутри Expander «Подробнее»).
        - Persist: `state\\operator_sessions.json` (best-effort) + env override `ISP_AUDIT_OPERATOR_SESSIONS_PATH`, cap/size.
        - Дедуп: сессия закрывается итогом; если пользователь запускает новую проверку до завершения пост-apply — предыдущая закрывается best-effort как UNKNOWN.
      - [ ] История событий: фильтры (можно staged, сначала самый простой):
        - [x] по сервису/группе
        - [x] по типу (только вмешательства / только исправления / только ошибки)
        - [x] по времени (сегодня/7 дней/всё)
      - [ ] Явные действия вместо «скрытых» (без double-click как единственного способа увидеть детали): кнопка/ссылка «Подробнее».
      - [ ] Сводка вместо таблицы по умолчанию: «проверено N / проблемных M», список проблем — карточками.
      - [ ] Сортировка/приоритизация: сначала блокировки/провалы, затем предупреждения, OK — скрыть или свернуть.
    - [ ] Автопилот (execution policy):
      - [ ] Правила авто‑применения на базе INTEL/diagnosis (confidence/risk) + debounce/anti‑storm.
      - [ ] По умолчанию: только селективные/обратимые действия (например QUIC→TCP по target IP, TTL endpoint block, TCP/80 host tricks).
      - [ ] Запрещено по умолчанию: DNS/DoH и глобальные воздействия (требуют явного consent‑диалога).
      - [ ] Эскалация ступенями: применить → post‑apply retest → при неуспехе предложить “усилить” (одно действие, без выбора стратегии).
      - [ ] Всегда доступен быстрый rollback («Откатить всё» + auto‑cleanup DNS/DoH).
      - [ ] Конфликт Autopilot vs ручные действия (Operator/Engineer):
        - [ ] Ввести ownership в транзакциях/состоянии: `appliedBy=autopilot|user` (или аналог) + `scope` (группа/цель).
        - [ ] Правило rollback в Operator: откатывает только то, что включил Autopilot (по ownership), не ломая ручные настройки Engineer.
        - [ ] Явная кнопка «Снять всё (включая ручное)» — только в Engineer и с подтверждением.
    - [ ] Визуальный дизайн (MaterialDesign):
      - [ ] Тёмная тема по умолчанию в Operator UI (опционально авто‑переключение только для него).
      - [ ] Hero‑элемент: большой индикатор (щит/радар/алерт) + крупный статус‑текст.
      - [ ] Компоновка Operator экрана (простая SPA-подобная):
        - [ ] Header (минимальный)
        - [ ] Главный статус (центр)
        - [ ] Одна главная кнопка (центр)
        - [ ] Детали (Expander: «Результаты проверки», Expander: «Технический лог»)
        - [ ] Footer: переключатель обхода как вторичное действие (Switch) + «Вкл/Выкл»
      - [x] «История активности» как Expander/панель карточек, по клику — раскрытие тех. деталей (JSON) для репорта.
      - [ ] Компактный индикатор «последний статус» (серый: не проверено, зелёный/жёлтый/красный после теста).
      - [ ] Метафора Operator UI (выбрать и придерживаться): «щит/командный центр/бортовой щит корабля» — три главных состояния: норма / сканирование / атака.
    - [ ] Локализация/тексты (операторский словарь):
      - [ ] Словарь “код → человеческий текст” для основных причин (DNS/TCP/TLS/QUIC/RST/Redirect) + короткая рекомендация.
      - [ ] Единые формулировки CTA и статусов (без жаргона: QUIC/DoH можно упоминать только в “Подробнее”).
    - [ ] Тесты/смоки:
      - [ ] Добавить smoke сценарий запуска Operator UI (non-admin) + проверка переключения в Engineer.
      - [x] Smoke/ui: `UI-015` — round-trip истории Operator (persist/load `state/operator_events.json`).
      - [ ] Регресс на shutdown: при закрытии Operator окна гарантированно вызывается безопасный `ShutdownAsync` (DNS/DoH rollback).
      - [ ] Добавить минимальные unit/регресс тесты на AutoPilot policy (не допускаем DNS/DoH без consent).

---

## P2 — Улучшения UX

- 🟢 P2.1: AutoRetest debounce
  - Чек‑лист:
    - [ ] Ограничить частоту ретестов/авто-действий, чтобы не провоцировать гонки и шум.

- 🟢 P2.2: Early noise filter
  - Чек‑лист:
    - [ ] Раннее подавление “шумных” карточек и ложных срабатываний до отображения в UI.

- 🟢 P2.3: Progress indicator для Apply
  - Чек‑лист:
    - [x] Показ фаз Apply и текущего прогресса (индикатор + текущая фаза ApplyIntelPlan в UI).

- 🟢 P2.4: История транзакций для карточки
  - Чек‑лист:
    - [ ] История apply/rollback/retest по выбранной карточке (не только последняя транзакция).

## P0.2 — Policy-driven: незакрытое

- 🟡 Advanced режим: ручное создание/редактирование политик (опционально)
  - UI/UX для безопасного ручного управления FlowPolicy (с валидацией и понятными предупреждениями).

- 🟡 Замеры и лимиты
  - Измерить overhead lookup в decision graph на hot path.
  - Ограничения по памяти/количеству политик (cap) и политика деградации.
  - Асинхронная перекомпиляция графа без блокировки UI.

---

## Стратегии обхода: долги/блокеры

- 🔴 BadChecksum (2.2): стратегия реализована, но ограничена по смыслу (работает только для фейковых пакетов) — оформить в UX/доках и зафиксировать критерии/ожидаемый эффект.
- 🟡 QuicObfuscation (2.3): сейчас маппится на QUIC→TCP (DROP UDP/443); реальную обфускацию QUIC (если потребуется) оформить как отдельную технику/политику.
- 🟡 HttpHostTricks: базовая реализация есть и применяется; нужен следующий уровень интеграции в semantics/наблюдаемость как управляемой политики.
- 🟡 Auto-hostlist: подключён как контекст, но пока не влияет на постановку диагноза/плана.

---

## Фаза 4 — Рефакторинг и технический долг

- 🟡 Доминирование/веса планов (INTEL)
  - Если план A активен, планы B где B ⊂ A не рассматриваются.
  - Разделить ранжирование на strength/cost и учитывать feedback (успех/провал).

- 🟡 DI container
  - Переход на Microsoft.Extensions.DependencyInjection; уход от ручного wiring.

- 🟡 Устранение глобального состояния
  - Перевести Config.ActiveProfile/Program.Targets в сервисы.

- 🟡 Декомпозиция DiagnosticOrchestrator (следующий шаг после partial)
  - Выделение компонентов: Pipeline / Recommendations / CardActions / State.

---

## Фаза 5 — Native Core (Rust DLL)

- 🟢 5.0: Инфраструктура Rust
  - 5.0.3: Настроить MSBuild для автоматической сборки Rust DLL при `dotnet build`.
  - 5.0.4: Добавить копирование `isp_audit_native.dll` в output directory.

- 🟡 5.1: WinDivert FFI обёртка на Rust
  - Реализовать `DivertIpHdr`, `DivertTcpHdr`.
  - Реализовать `divert_calc_checksums` с валидацией буфера.
  - Создать `WinDivertNativeRust.cs` + feature flag переключения старый/новый interop.

- 🟡 5.2: Packet parser на Rust
  - Zero-copy парсинг IP/TCP/UDP с bounds checking и интеграцией в hot path.

- 🟢 5.3: TLS/SNI parser на Rust
  - Безопасный парсинг ClientHello + извлечение SNI.

- 🟢 5.4: Опционально — выделить bypass в отдельную .NET DLL


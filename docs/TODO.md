# ISP_Audit — TODO (Открытые проблемы и задачи)

Дата: 04.12.2025
Назначение: единый список нерешённых проблем и задач. Всё реализованное и историческое остаётся в Changelog и архитектурном документе.

---

## Приоритеты
- 🔴 Критический: влияет на корректность детекции/обхода
- 🟡 Важный: улучшает точность/надёжность
- 🟢 Низкий: UX/полиш, интеграции

---

## Проблемы (Критические)
1. Детекция блокировок неполная (🔴)
  - Нет счётчика TCP retransmissions (главный индикатор DPI)
  - Нет детекции HTTP redirect на другой домен
  - Нет инспекции RST-пакетов на уровне payload/flags
  - Нет fail counter с окном времени
  - Нет фильтрации по PID (приложению) в диагностике/обходе
2. Методы обхода ограничены (🔴)
  - Реализовать все ключевые стратегии в рамках WinDivert (без интеграции с GoodbyeDPI/Zapret)
  - Паритет по функциональности: bad checksum/seq, HTTP Host трюки, QUIC обфускация, TTL/fake, fragment/disorder
  - Добавить авто-фильтрацию hostlist и auto-hostlist

---

## Фаза 1 — Ядро: детекция + PID + стратегии (Критический приоритет)
**Статус:** ✅ Выполнено (v0.5.2-alpha)

- [x] Задача 1.1: TCP Retransmission tracking (🔴, ⏱️ 4–6ч)
  - Отслеживать TCP seq/ack, считать ретрансмиссии per-host
  - Порог 3 → метка DPI-блокировки
  - Интеграция в `StandardBlockageClassifier`
- [x] Задача 1.2: HTTP redirect detection (🔴, ⏱️ 2–3ч)
  - Парсинг HTTP ответов, `Location`/`Host`
  - SLD отличается → `HTTP_REDIRECT`
- [x] Задача 1.3: Fail counter + time window (🟡, ⏱️ 1ч)
  - N фейлов за M секунд → блокировка (default: 3/60)
  - Сброс при успехе, параметры конфигурируемые
- [x] Задача 1.4: RST packet inspection (🟡, ⏱️ 2ч)
  - Выделить провайдерские RST по характерным признакам
- [x] Задача 1.5: Фильтрация по PID (🔴, ⏱️ 3–4ч)
  - Связать соединения/пакеты с процессами, UI фильтрация по приложению
  - Ограничивать применение стратегий по выбранным PID
- [x] Задача 1.6: Базовые стратегии обхода (🔴, ⏱️ 8–12ч)
  - Реализовано: TLS Fragment, TLS Disorder, TTL/Fake, Drop RST
  - Интеграция с `TrafficEngine` и `BypassController`
  - Hostlist: авто-пополнение/фильтрация по результатам детекции (частично)

---

## Фаза 2 — Расширение обхода (Средний приоритет)
**Статус:** ⏳ В ожидании

### Фаза 2 (v2) — Доработки после MVP (Signals → Diagnosis → Strategy → Executor)
**Статус:** ⏳ В работе / план

- [ ] Задача 2.V2.1: Реальный Executor (ручной запуск, без auto-apply) (🔴)
  - Цель: заменить MVP-логирование на реальное применение плана ТОЛЬКО по явному действию пользователя.
  - Минимальный контракт:
    - Команда/кнопка: «Применить рекомендации v2».
    - Преобразование `BypassPlan` → вызовы `BypassController`/`TlsBypassService`.
    - Отмена через `CancellationToken`, таймаут, безопасный откат/Disable.
  - Gate:
    - Без клика пользователя ничего не применяется.
    - Лог содержит понятный `Reasoning` + список стратегий.

- [ ] Задача 2.V2.2: Feedback Store (🟡)
  - Цель: запоминать исход (успех/неуспех) для пары (DiagnosisId + StrategyId) и использовать дальше для ранжирования.
  - MVP формат: in-memory + сохранение в файл (опционально) с ограничением размера.
  - Gate:
    - При отсутствии данных поведение полностью как сейчас.
    - Данные не растут бесконечно (TTL/лимит записей).

- [ ] Задача 2.V2.3: Ранжирование по feedback (🟡)
  - Цель: поверх hardcoded приоритетов добавлять вес по успешности.
  - Gate:
    - Жёсткие защиты сохраняются: `confidence < 50` → пустой план; `RiskLevel.High` запрещён при `confidence < 70`.
    - Детерминизм: одинаковый вход + одинаковый feedback → одинаковый план.

- [ ] Задача 2.V2.4: Усилить сенсоры под DPI-диагнозы (🟡)
  - Цель: чтобы `ActiveDpiEdge/StatefulDpi` опирались на устойчивые улики (RST TTL delta/latency, инъекции), а не только флаги.
  - Gate:
    - На «рабочих» целях не появляются уверенные DPI-диагнозы.
    - На «проблемных» целях уменьшается доля `Unknown`.

- [ ] Задача 2.V2.5: Auto-hostlist (2.4) — участие в v2 (🟡)
  - Цель: подключить auto-hostlist как источник фактов/контекста для v2 (evidence/notes, далее — вход в диагноз).
  - Gate:
    - Нет регрессий производительности.

- [ ] Задача 2.V2.6: Реализовать отложенные стратегии (🟡)
  - HTTP Host tricks (2.1)
  - QUIC obfuscation (2.3)
  - Bad checksum (2.2) — только после снятия блокера на уровне движка

- [ ] Задача 2.V2.LAST: Открытый вопрос — `DnsHijack` → стратегии (🟢, оставить на последнее)
  - Зафиксировать ожидаемое поведение: только подсказка/ручная рекомендация vs low-risk рекомендация `UseDoh`.
  - Привести к одному варианту: `StandardStrategySelectorV2` + тексты UI + документация.

- [ ] Задача 2.1: HTTP Host трюки (🟡, ⏱️ 4–6ч)
  - Манипуляции заголовком `Host`/`SNI`: вставка пробелов, регистр, дублирование, скрытая упаковка
  - Варианты обхода DPI на уровне HTTP 1.1
- [ ] Задача 2.2: Bad checksum/sequence (🟡, ⏱️ 6–8ч)
  - Генерация пакетов с «плохой» контрольной суммой/последовательностью для запутывания DPI
  - Акуратная реализация с контролем стабильности соединений
- [ ] Задача 2.3: QUIC/UDP обфускация (🟡, ⏱️ 6–10ч)
  - Фрагментация/псевдо-дезорганизация первых QUIC пакетов, изменение полей, тайминги
  - Тестирование на YouTube/Discord
- [ ] Задача 2.4: Auto-hostlist (🟡, ⏱️ 3–4ч)
  - Автопополнение списка хостов на основе детекции (ретрансмиссии/редиректы)
  - Фильтрация и приоритизация по частоте блокировок
- [ ] Задача 2.5: Autottl/параметры TTL Fake (🟡, ⏱️ 2–3ч)
  - Подбор оптимального TTL/Fake режимов по результатам тестов
  - Профили для разных провайдеров
- [ ] Задача 2.6: Параметризация TLS Fragment/Disorder (🟡, ⏱️ 2–3ч)
  - Гибкая настройка размеров и порядка фрагментов ClientHello
  - Эксперименты для устойчивости к обходу DPI


Примечание: архитектурный справочник и реализованные изменения см. в `ARCHITECTURE_CURRENT.md` и `CHANGELOG.md`.

---

## Фаза 4 — Рефакторинг и Технический долг (Q1 2026)
**Приоритет:** 🔵 Низкий
**Цель:** Улучшение поддерживаемости кода и архитектуры.

- Задача 4.1: Внедрение DI Container
  - Переход на `Microsoft.Extensions.DependencyInjection`.
  - Уход от ручного создания зависимостей в `MainViewModel` и `App.xaml.cs`.
- Задача 4.2: Устранение глобального состояния
  - Рефакторинг `Config.ActiveProfile` (перевод в `IConfigService`).
  - Рефакторинг `Program.Targets` (перевод в `ITargetService`).
- Задача 4.3: Рефакторинг `DiagnosticOrchestrator`
  - Разделение на `StartupManager`, `OverlayManager`, `ProcessMonitor`.
- Задача 4.4: Рефакторинг `TestResultsManager`
  - Выделение логики парсинга сообщений в отдельный сервис.

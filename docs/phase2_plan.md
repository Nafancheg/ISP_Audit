# Фаза 2 — План реализации (Расширение обхода)

## Цели
- Увеличить покрытие стратегий обхода DPI/провайдерских фильтров без интеграции сторонних инструментов.
- Сделать настройки гибкими для разных провайдеров (TTL/Fake, фрагментация TLS/QUIC, hostlist).
- Минимизировать регрессии: все новые стратегии должны быть опциональными и управляться профилем/флагами в UI.

## Приоритет (что сделать быстрее)
1) **Быстрые победы (низкий риск, быстрый результат)**
   - 2.6 Параметризация TLS Fragment/Disorder (🟡, 2–3ч): вынести размеры/порядок в профиль + пресеты.
   - 2.5 Autottl/параметры TTL Fake (🟡, 2–3ч): подбор TTL/FAKE через быстрый брут по малому набору хостов.
2) **Среднесрочно**
   - 2.4 Auto-hostlist (🟡, 3–4ч): автопополнение из детекции (ретрансмиссии/редиректы) + фильтр по частоте.
   - 2.1 HTTP Host трюки (🟡, 4–6ч): манипуляции Host/SNI, конфигурируемые варианты.
3) **Дольше всего (нужны эксперименты)**
   - 2.2 Bad checksum/sequence (🟡, 6–8ч): генерация «плохих» пакетов с контролем стабильности.
   - 2.3 QUIC/UDP обфускация (🟡, 6–10ч): фрагментация/дезорганизация первых пакетов QUIC, тайминги.

## Дорожная карта и инкременты
- **Инкремент A (быстрый выпуск)**: 2.6 + 2.5. Результат: гибкие пресеты фрагментации TLS и автоматический подбор TTL/Fake; минимальные изменения UI (добавить пресеты/флажки).
- **Инкремент B**: 2.4 + 2.1. Результат: автопополняемый hostlist и HTTP Host трюки (список вариантов, включаются выбором профиля/флажков).
- **Инкремент C**: 2.2. Результат: стратегия bad checksum/seq с защитой от дестабилизации соединения.
- **Инкремент D**: 2.3. Результат: режимы обфускации QUIC для YouTube/Discord, отключаемые по умолчанию.

## План выполнения (актуализировано по коду на 15.12.2025)

### Инкремент A (2.6 + 2.5)
- [x] 2.6: TLS Fragment/Disorder параметризация (реализовано, см. статус ниже).
- [x] 2.5.1: Исправить профиль обхода: сохранять и загружать `TtlTrick`/`TtlTrickValue` в `bypass_profile.json`.
  - Критерий готовности: после `Save()` поля присутствуют в JSON; после перезапуска значения восстанавливаются.
- [x] 2.5.2: Сделать TTL-параметры управляемыми в рантайме через `TlsBypassService` (не только из базового профиля).
  - Критерий готовности: изменение TTL в опциях приводит к применению нового TTL trick без перезапуска.
- [x] 2.5.3: Реализовать AutoTTL-подбор (малый перебор TTL 2..8) по метрикам bypass на реальном TLS@443 трафике.
  - Критерий готовности: после подбора выбранный TTL сохраняется в `bypass_profile.json` и используется в следующих запусках.
  - Примечание: включается через `autoTtl: true` в `bypass_profile.json` (по умолчанию выключено).

### Инкремент B (2.4 + 2.1)
- [ ] 2.4: Auto-hostlist на базе уже существующих сигналов (ретрансмиссии/HTTP-редиректы/RST/UDP) + лимит размера и фильтр по частоте.
  - Критерий готовности: список не растёт бесконечно, дубликаты не добавляются, шум фильтруется.
- [ ] 2.1: HTTP Host трюки — добавлять только после появления hostlist/пер-хост настроек.
  - Критерий готовности: стратегия выбирается на уровне хоста (профиль/hostlist), режим по умолчанию выключен.

### Инкремент C (2.2)
- [ ] 2.2.0 (блокер): добавить в `TrafficEngine` управляемый режим отправки пакетов без обязательного пересчёта checksum (иначе «bad checksum» физически не достигается).
  - Критерий готовности: для специально инъецируемых пакетов checksum не исправляется движком.
- [ ] 2.2.1: отделить «bad sequence» (уже есть в Fake/SEQ) от «bad checksum» и добавить автозащиту/rollback.
  - Критерий готовности: режим сам отключается при признаках деградации и отражает это в метриках/логе.

### Инкремент D (2.3)
- [ ] 2.3.0 (архитектурная база): добавить механизм таймингов/задержанной отправки для UDP (без блокировки основного цикла перехвата).
  - Критерий готовности: фильтр может «дропнуть оригинал и отправить модифицированные UDP пакеты позже».
- [ ] 2.3.1: QUIC/UDP обфускация (фрагментация/разупорядочивание/задержки) per-host, выключено по умолчанию.
  - Критерий готовности: включение per-host приводит к применению схемы обфускации только для выбранных целей.

## Детализация задач
- 2.6 TLS Fragment/Disorder
  - Вынести параметры (размеры чанков, порядок) в профиль `BypassProfile` + UI пресеты.
  - Добавить безопасные дефолтные пресеты и выключение по умолчанию.
  - Тест: сравнить успешность TLS ClientHello с/без пресетов на тест-хостах.
  - **Статус (выполнено):**
    - TLS bypass вынесен в `TlsBypassService`: сервис сам регистрирует/удаляет `BypassFilter`, держит пресеты, нормализует опции и шлёт `MetricsUpdated/VerdictChanged/StateChanged`; `BypassController` стал тонким прокси (тумблеры + сохранение пресета, без таймера).
    - Параметры фрагментации сохраняются в `bypass_profile.json`; пресеты Стандартный/Умеренный/Агрессивный/Профиль и выбранный пресет восстанавливаются из профиля, валидация размеров (мин. 4 байта, сумма в [threshold..16384], иначе откат к безопасному списку).
    - Метрики из сервиса: TLS обработано/фрагментировано, релевантные RST, все RST, последний план фрагментов, с какого времени считаются метрики, сколько ClientHello увидено/коротких/не 443, активный пресет, порог и минимальный чанк; UI читает только события сервиса.
    - Вердикты сервиса: нет TLS 443 в трафике (серый), TLS уходит не на 443 (серый), ClientHello короче threshold (жёлтый, совет снизить порог/выбрать агрессивный пресет), обход активен, но не применён (красный), мало данных <10 (серый), ratio RST/фрагм >4 (красный) или >1.5 (жёлтый), иначе зелёный.
    - Автокоррекция (флаг UI `AutoAdjustAggressive`): только для пресета «Агрессивный»; при раннем росте RST ужимает минимальный чанк до 4, при устойчивом зелёном >30с слегка уменьшает минимальный чанк (не ниже 4) и переприменяет опции через сервис.
    - Ограничения: фрагментация/дезорганизация применяется только к TLS ClientHello на порту 443 с SNI и длиной ≥ threshold; короткие/не443 ClientHello считаются отдельно и дают серые/жёлтые статусы; «нет данных» не подсвечивает карточку проблем.
    - Дефолт: фрагментация выключена до выбора пресета, Drop RST выключен, пресет «Профиль» подхватывает сохранённый список, агрессивный пресет ограничен минимумом 4 байта.
- 2.5 Autottl / TTL Fake
  - Реализовать быстрый перебор TTL/Fake на 2–3 хостах, зафиксировать лучший профиль в сессии.
  - Экспортировать в профиль параметры TTL и статус выбранного пресета.
  - Тест: метрика — уменьшение TCP RST/ретрансмиссий на выбранных хостах.
  - **План реализации (конкретные шаги по коду):**
    - Добавить сохранение/загрузку `TtlTrick`/`TtlTrickValue` (и флага `autoTtl`) в `bypass_profile.json`.
    - Прокинуть TTL-параметры в слой применения опций (`TlsBypassService`), чтобы AutoTTL мог менять TTL в рантайме.
    - Реализовать процедуру подбора TTL по метрикам обхода (малый набор TTL, окно измерения, выбор лучшего, сохранение в профиль).
- 2.4 Auto-hostlist
  - Источники: ретрансмиссии > порога, HTTP редиректы на другой домен.
  - Фильтр по частоте и лимит размера; UI: показать новые хосты и дать toggle.
  - Тест: список не растёт бесконечно, дубликаты/шум фильтруются.
- 2.1 HTTP Host трюки
  - Реализовать варианты: пробел после двоеточия, mixed-case Host, дублирование Host, инъекция скрытых символов в SNI (там, где допустимо), padding до длины.
  - Сделать выбор стратегии в профиле/hostlist (пер-хост параметры).
  - Тест: проверки на совместимость с популярными CDN (не ломаем TLS).
- 2.2 Bad checksum/sequence
  - Добавить режимы в `TrafficEngine`: инъекция пакетов с плохой checksum/seq, контроль rollback при нестабильности.
  - Флаг устойчивости: авто-отключение режима при росте потерь/RTT.
  - Тест: нагрузочный тест на стабильность TCP-сессии.
  - **Блокер по текущей реализации:** пока `TrafficEngine` пересчитывает checksum перед отправкой, режим «bad checksum» не достигается — требуется явный режим отправки без пересчёта для специально инъецируемых пакетов.
- 2.3 QUIC/UDP обфускация
  - Фрагментация/разупорядочивание первых пакетов QUIC, задержки между отправками.
  - Профиль: отдельный флажок per-host; безопасный дефолт — выключено.
  - Тест: YouTube/Discord стартовые соединения, замер успеха/latency.

## Готовность к выкладке и контроль рисков
- Все новые режимы — выключены по умолчанию, включение только через профиль или UI-флажки.
- Добавить метрики: счётчик отключений режимов по автозащите (для bad checksum/seq, QUIC).
- Логи: отчёт о выбранных пресетах (TTL/TLS), новых хостах в auto-hostlist.
- Тестовый чеклист перед релизом инкремента: сборка, авто-тесты LiveTestingPipeline, ручные проверки на 3–4 хостах.

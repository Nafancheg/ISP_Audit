namespace IspAudit;

public partial class App : System.Windows.Application
{
    protected override void OnStartup(System.Windows.StartupEventArgs e)
    {
        base.OnStartup(e);

        // Targets загружаются статически в TargetCatalog через конструктор
    }
}

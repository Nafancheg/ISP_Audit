<Window x:Class="ISPAudit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ISPAudit.Controls"
        xmlns:vm="clr-namespace:ISPAudit.ViewModels"
        Title="ISP Audit - Диагностика сети"
        Height="800" Width="1400"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="Manual"
        Loaded="Window_Loaded"
        Background="{StaticResource BgBrush}"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">

    <Window.DataContext>
        <vm:MainViewModelRefactored/>
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Fix Status Bar убрана - статус виден по кнопкам Bypass Control -->

        <!-- Scenario Selector -->
        <Border Grid.Row="1" 
                Style="{StaticResource CardStyle}" 
                Padding="16" 
                Margin="0,0,0,12"
                AutomationProperties.AutomationId="ScenarioSelector">
            <StackPanel>

                <!-- Exe Selection -->
                <StackPanel Margin="0,0,0,0" 
                            IsEnabled="{Binding IsBasicTestMode, Converter={StaticResource InverseBoolConverter}}"
                            AutomationProperties.AutomationId="ExeScenarioPanel">
                    <TextBlock Text="Выберите exe файл игры или лаунчера" Style="{StaticResource BodyStyle}" Margin="0,0,0,4"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Style="{StaticResource ModernTextBoxStyle}" 
                                 Text="{Binding ExePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AutomationProperties.AutomationId="ExePathTextBox" Margin="0,0,750,10"/>
                        <Button Content="Обзор..." 
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding BrowseExeCommand}"
                                IsEnabled="{Binding IsStart}"
                                Margin="600,0,550,10"
                                AutomationProperties.AutomationId="BrowseButton"/>
                        <CheckBox Content="Тест базовых сервисов (Google, YouTube, Discord)"
                                  IsChecked="{Binding IsBasicTestMode}"
                                  IsEnabled="{Binding IsStart}"
                                  AutomationProperties.AutomationId="BasicTestCheckbox" Margin="1002,14,20,21"/>
                    </Grid>
                </StackPanel>

                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto" MinWidth="198.4"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,198,0">
                        <!-- Basic Services Test Checkbox -->

                        <!-- Auto Bypass Options - СКРЫТО: bypass включается автоматически при старте
                             Переключение стратегий будет реализовано отдельно -->
                    </StackPanel>

                    <!-- Start Button (Unified) -->
                    <Button
                            Content="{Binding StartButtonText}" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding StartLiveTestingCommand}"
                            VerticalAlignment="Top"
                            Margin="788,-60,149,0"
                            AutomationProperties.AutomationId="StartButton" Height="40"/>
                </Grid>

                <!-- Bypass Control Panel (Пульт управления) -->
                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" 
                        Margin="0,16,0,0" Padding="16,12"
                        Visibility="{Binding ShowBypassPanel, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <!-- Header Row -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Title -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="🎛️" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Bypass Control" FontWeight="Bold" FontSize="14" Foreground="#1F2937" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Status Badges -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <!-- DoH Active Badge -->
                                <Border Background="#DBEAFE" CornerRadius="6" Padding="10,4" Margin="0,0,8,0"
                                        Visibility="{Binding IsDoHActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔒" FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="DoH (Cloudflare)" FontWeight="SemiBold" FontSize="12" Foreground="#1E40AF"/>
                                    </StackPanel>
                                </Border>
                                <!-- Bypass Active Badge -->
                                <Border Background="#DCFCE7" CornerRadius="6" Padding="10,4"
                                        Visibility="{Binding IsBypassActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="●" Foreground="#22C55E" FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding CurrentBypassStrategy}" FontWeight="SemiBold" FontSize="12" Foreground="#166534"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <!-- VPN Warning -->
                        <Border Background="#FEF3C7" BorderBrush="#FCD34D" BorderThickness="1" CornerRadius="6" 
                                Padding="10,6" Margin="0,0,0,8"
                                Visibility="{Binding IsVpnDetected, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔒" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding VpnWarningText}" FontSize="12" Foreground="#92400E" VerticalAlignment="Center" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <!-- Compatibility Warning (несовместимость стратегий) -->
                        <Border Background="#FEF9C3" BorderBrush="#FDE047" BorderThickness="1" CornerRadius="6" 
                                Padding="10,6" Margin="0,0,0,12"
                                Visibility="{Binding HasCompatibilityWarning, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="{Binding CompatibilityWarning}" FontSize="11" Foreground="#854D0E" 
                                       VerticalAlignment="Center" TextWrapping="Wrap" LineHeight="18"/>
                        </Border>

                        <!-- Независимые toggle-кнопки bypass (1 опция = 1 кнопка) -->
                        <Grid Margin="0,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Fragment -->
                            <Button Grid.Column="0" Command="{Binding ToggleFragmentCommand}"
                                    Height="36" FontSize="11" FontWeight="Medium" Cursor="Hand" Margin="0,0,4,0"
                                    ToolTip="TLS_FRAGMENT&#x0a;Фрагментация ClientHello (обычный порядок)">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                        <Setter Property="Content" Value="TLS_FRAGMENT"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFragmentEnabled}" Value="True">
                                                <Setter Property="Background" Value="#3B82F6"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#2563EB"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Disorder (фрагменты в обратном порядке) -->
                            <Button Grid.Column="1" Command="{Binding ToggleDisorderCommand}"
                                    Height="36" FontSize="11" FontWeight="Medium" Cursor="Hand" Margin="0,0,4,0"
                                    ToolTip="TLS_DISORDER&#x0a;Фрагменты в ОБРАТНОМ порядке&#x0a;(сначала 2-й, потом 1-й) — эффективнее против DPI">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                        <Setter Property="Content" Value="TLS_DISORDER"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDisorderEnabled}" Value="True">
                                                <Setter Property="Background" Value="#8B5CF6"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#7C3AED"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Fake -->
                            <Button Grid.Column="2" Command="{Binding ToggleFakeCommand}"
                                    Height="36" FontSize="11" FontWeight="Medium" Cursor="Hand" Margin="0,0,4,0"
                                    ToolTip="TLS_FAKE&#x0a;Fake TTL пакет для обхода active probing">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                        <Setter Property="Content" Value="TLS_FAKE"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFakeEnabled}" Value="True">
                                                <Setter Property="Background" Value="#3B82F6"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#2563EB"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- DROP RST -->
                            <Button Grid.Column="3" Command="{Binding ToggleDropRstCommand}"
                                    Height="36" FontSize="11" FontWeight="Medium" Cursor="Hand" Margin="0,0,4,0"
                                    ToolTip="DROP_RST&#x0a;Блокировка TCP RST пакетов от провайдера">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                        <Setter Property="Content" Value="DROP_RST"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDropRstEnabled}" Value="True">
                                                <Setter Property="Background" Value="#3B82F6"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#2563EB"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- DoH -->
                            <Button Grid.Column="4" Command="{Binding ToggleDoHCommand}"
                                    Height="36" FontSize="12" FontWeight="Medium" Cursor="Hand" Margin="0,0,8,0"
                                    ToolTip="DoH (DNS-over-HTTPS)&#x0a;Защита DNS через Cloudflare 1.1.1.1">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                        <Setter Property="Content" Value="🔒 DoH"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDoHEnabled}" Value="True">
                                                <Setter Property="Background" Value="#3B82F6"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#2563EB"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Выключить всё -->
                            <Button Grid.Column="5" Command="{Binding DisableAllBypassCommand}"
                                    Height="36" Width="80" FontSize="12" FontWeight="SemiBold" Cursor="Hand"
                                    ToolTip="Отключить все опции bypass">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                        <Setter Property="Content" Value="✕ Выкл"/>
                                        <Setter Property="Background" Value="#FEE2E2"/>
                                        <Setter Property="Foreground" Value="#DC2626"/>
                                        <Setter Property="BorderBrush" Value="#FECACA"/>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Test Results -->
        <Border Grid.Row="2" 
                Style="{StaticResource CardStyle}" 
                Padding="0"
                AutomationProperties.AutomationId="TestResults">
            <Grid>
                <!-- Start State -->
                <StackPanel Visibility="{Binding IsStart, Converter={StaticResource BoolToVisibilityConverter}}"
                            VerticalAlignment="Center" 
                            HorizontalAlignment="Center"
                            Margin="16"
                            AutomationProperties.AutomationId="StartState">
                    <TextBlock Text="Готов к диагностике" Style="{StaticResource H2Style}" 
                               Foreground="{StaticResource MutedBrush}" TextAlignment="Center"/>
                    <TextBlock Text="Выберите сценарий и нажмите 'Начать диагностику'" 
                               Style="{StaticResource BodyStyle}"
                               Foreground="{StaticResource MutedBrush}"
                               TextAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Active State (Running OR Done) -->
                <Grid Visibility="{Binding IsStart, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Header (Running) -->
                        <RowDefinition Height="Auto"/>
                        <!-- Header (Done) -->
                        <RowDefinition Height="*"/>
                        <!-- DataGrid -->
                    </Grid.RowDefinitions>

                    <!-- Running Header -->
                    <StackPanel Grid.Row="0" 
                                Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="16,16,16,8">

                        <!-- Title Row with Badge -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Выполняется диагностика..." Style="{StaticResource H2Style}" VerticalAlignment="Center"/>

                            <!-- Bypass Badge - показывает что bypass активен -->
                            <Border Background="#DCFCE7" BorderBrush="#86EFAC" BorderThickness="1" CornerRadius="4" Margin="12,0,0,0" Padding="6,2"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding IsBypassActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🛡️ Bypass Active" Foreground="#166534" FontWeight="SemiBold" FontSize="11"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <TextBlock Text="{Binding CurrentAction}" 
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,8"
                                   TextWrapping="Wrap"
                                   Visibility="{Binding CurrentAction, Converter={StaticResource StringToVisibilityConverter}}"/>
                        <ProgressBar IsIndeterminate="True"
                                     Height="2" 
                                     BorderThickness="0"
                                     Background="#E5E7EB"
                                     Foreground="{StaticResource PrimaryBrush}"/>

                        <!-- D2: Flow Stats -->
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="Активность сети:" FontWeight="SemiBold" Margin="0,0,8,0" Foreground="#4B5563" FontSize="12"/>
                            <TextBlock Text="{Binding FlowModeText, StringFormat='Режим: {0}'}" Margin="0,0,12,0" Foreground="#4B5563" FontWeight="Medium" FontSize="12"/>
                            <TextBlock Text="{Binding ConnectionsDiscovered, StringFormat='Соединений: {0}'}" Margin="0,0,12,0" Foreground="#374151" FontSize="12"/>
                            <TextBlock Text="{Binding FlowEventsCount, StringFormat='Событий Flow: {0}'}" Foreground="#6B7280" FontSize="12"/>
                        </StackPanel>

                        <!-- Health Indicator -->
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Стабильность (1 мин):" FontSize="12" Foreground="#4B5563" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <ProgressBar Grid.Column="1" 
                                         Value="{Binding ResultsManager.HealthScore, Mode=OneWay}" 
                                         Maximum="100" 
                                         Height="6" 
                                         BorderThickness="0"
                                         Background="#E5E7EB"
                                         Foreground="{Binding ResultsManager.HealthColor}"
                                         VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" 
                                       Text="{Binding ResultsManager.HealthScore, StringFormat='{}{0:F0}%'}" 
                                       FontSize="12" 
                                       FontWeight="SemiBold" 
                                       Foreground="{Binding ResultsManager.HealthColor}"
                                       Margin="8,0,0,0" 
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>

                    <!-- Done Header -->
                    <StackPanel Grid.Row="1" 
                                Visibility="{Binding IsDone, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="16,16,16,8">
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Диагностика завершена" Style="{StaticResource H2Style}"/>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="Экспорт отчета" 
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        Command="{Binding ReportCommand}"
                                        Margin="0,0,8,0"
                                        Padding="10,4"
                                        FontSize="12"/>
                                <Button Content="Новая диагностика" 
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Command="{Binding SetStateCommand}"
                                        CommandParameter="start"
                                        Padding="10,4"
                                        FontSize="12"/>
                            </StackPanel>
                        </Grid>

                        <!-- Health Indicator (Final) -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Итоговая стабильность:" FontSize="12" Foreground="#4B5563" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <ProgressBar Grid.Column="1" 
                                         Value="{Binding ResultsManager.HealthScore, Mode=OneWay}" 
                                         Maximum="100" 
                                         Height="8" 
                                         BorderThickness="0"
                                         Background="#E5E7EB"
                                         Foreground="{Binding ResultsManager.HealthColor}"
                                         VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" 
                                       Text="{Binding ResultsManager.HealthScore, StringFormat='{}{0:F0}%'}" 
                                       FontSize="12" 
                                       FontWeight="SemiBold" 
                                       Foreground="{Binding ResultsManager.HealthColor}"
                                       Margin="8,0,0,0" 
                                       VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Summary Stats -->
                        <!--
                        <Border Background="#F9FAFB" 
                                CornerRadius="8" 
                                Padding="12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0">
                                    <TextBlock Text="Успешно" Style="{StaticResource CaptionStyle}"/>
                                    <TextBlock Text="{Binding PassCount}" Style="{StaticResource H2Style}" Foreground="{StaticResource PassBrush}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1">
                                    <TextBlock Text="Ошибки" Style="{StaticResource CaptionStyle}"/>
                                    <TextBlock Text="{Binding FailCount}" Style="{StaticResource H2Style}" Foreground="{StaticResource FailBrush}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2">
                                    <TextBlock Text="Предупреждения" Style="{StaticResource CaptionStyle}"/>
                                    <TextBlock Text="{Binding WarnCount}" Style="{StaticResource H2Style}" Foreground="{StaticResource WarnBrush}"/>
                                </StackPanel>

                                <Border Grid.Row="1" Grid.ColumnSpan="3" 
                                        Margin="0,8,0,0" 
                                        Background="#ECFDF5" 
                                        BorderBrush="#A7F3D0" 
                                        BorderThickness="1" 
                                        CornerRadius="4" 
                                        Padding="8,4"
                                        Visibility="{Binding DiagnosticStatus, Converter={StaticResource StringToVisibilityConverter}}">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="💾" Margin="0,0,8,0" FontSize="12"/>
                                        <TextBlock Text="{Binding DiagnosticStatus}" Foreground="#047857" FontWeight="Medium" FontSize="12"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                        -->
                    </StackPanel>

                    <!-- Unified DataGrid -->
                    <DataGrid Grid.Row="2"
                              ItemsSource="{Binding TestResults}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserResizeRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              ClipboardCopyMode="IncludeHeader"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#E5E7EB"
                              VerticalGridLinesBrush="#E5E7EB"
                              Background="White"
                              BorderThickness="1"
                              BorderBrush="#E5E7EB"
                              RowBackground="White"
                              AlternatingRowBackground="#F9FAFB"
                              Padding="0"
                              FontSize="14"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto"
                              AutomationProperties.AutomationId="TestResultsDataGrid">

                        <DataGrid.Resources>
                            <!-- Override Selection Colors to Light Blue so buttons and gray text are visible -->
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#DBEAFE"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#F3F4F6"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black"/>

                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#374151"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E5E7EB"/>
                            </Style>

                            <Style TargetType="DataGridRow">
                                <Setter Property="MinHeight" Value="36"/>
                                <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#DBEAFE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="8,4"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" 
                                                    BorderBrush="Transparent" 
                                                    BorderThickness="0" 
                                                    Background="{TemplateBinding Background}" 
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <!-- Service Name -->
                            <DataGridTextColumn Header="Сервис" 
                                                Binding="{Binding Target.Name}" 
                                                Width="1.5*"
                                                MinWidth="120"
                                                FontWeight="SemiBold"/>

                            <!-- Host/IP -->
                            <DataGridTextColumn Header="Хост / IP" 
                                                Binding="{Binding Target.Host}" 
                                                Width="1*"
                                                MinWidth="100"/>

                            <!-- Status -->
                            <DataGridTemplateColumn Header="Статус" Width="1*" MinWidth="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <local:StatusDot Status="{Binding Status}" Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" FontWeight="Medium"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Details -->
                            <DataGridTextColumn Header="Детали" 
                                                Binding="{Binding Details}" 
                                                Width="3*"
                                                MinWidth="200">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Padding" Value="8,4"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="Foreground" Value="#4B5563"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Strategy -->
                            <DataGridTextColumn Header="Стратегия" 
                                                Binding="{Binding BypassStrategy}" 
                                                Width="1.2*"
                                                MinWidth="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="8,4"/>
                                        <Setter Property="Foreground" Value="#6B7280"/>
                                        <Setter Property="FontWeight" Value="Medium"/>
                                        <Setter Property="FontSize" Value="12"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Border>



        <!-- Footer: User Communication Area -->
        <Border Grid.Row="3" 
                Background="#F3F4F6" 
                CornerRadius="6" 
                Padding="12,8" 
                Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="ℹ️" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding UserMessage}" 
                           Style="{StaticResource BodyStyle}"
                           FontWeight="Medium"
                           Foreground="#374151"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

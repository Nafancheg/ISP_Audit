<Window x:Class="IspAudit.Windows.OperatorHelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Справка — Оператор"
        Height="420" Width="560"
        MinHeight="360" MinWidth="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14">

    <Grid Margin="12">
        <Border Style="{StaticResource CardStyle}" Padding="12">
            <StackPanel>
                <TextBlock Text="Справка" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

                <TextBlock TextWrapping="Wrap" Opacity="0.85">
                    <Run Text="Как пользоваться:"/>
                    <LineBreak/>
                    <Run Text="1) Нажмите «Проверить сеть» и дождитесь результата."/>
                    <LineBreak/>
                    <Run Text="2) Если найдены проблемы — нажмите «Исправить». Приложение применит безопасные действия и перепроверит."/>
                    <LineBreak/>
                    <Run Text="3) Если нужно больше технических деталей — откройте расширенный режим."/>
                </TextBlock>

                <Border Height="1" Background="{DynamicResource MaterialDesignDivider}" Margin="0,14,0,12"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Открыть расширенный режим"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Padding="16,6"
                            MinWidth="210"
                            Margin="0,0,10,0"
                            Click="OpenEngineer_Click"/>

                    <Button Content="Закрыть"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Padding="16,6"
                            MinWidth="120"
                            Click="Close_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>

<Window x:Class="IspAudit.Windows.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ISP Audit Overlay" 
        Height="180" Width="320"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Window.Resources>
        <DropShadowEffect x:Key="DropShadowEffect" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="#F0FFFFFF" 
            BorderBrush="#E5E7EB" 
            BorderThickness="1" 
            CornerRadius="12" 
            Padding="16"
            Effect="{StaticResource DropShadowEffect}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                <TextBlock Text="⏳" FontSize="16" Margin="0,0,8,0"/>
                <TextBlock Text="Диагностика сети" FontWeight="Bold" Foreground="#374151" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Message & Timer -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,12">
                <TextBlock x:Name="StatusText" Text="Ожидание соединений..." Foreground="#4B5563" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,4"/>
                
                <!-- Stats & Activity -->
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0,4,0,0">
                    <ProgressBar IsIndeterminate="True" Height="2" Width="200" Margin="0,0,0,6" Foreground="#3B82F6" Background="#E5E7EB" BorderThickness="0"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Соединений: " Foreground="#6B7280" FontSize="11"/>
                        <TextBlock x:Name="ConnectionsText" Text="0" FontWeight="Bold" Foreground="#374151" FontSize="11" Margin="0,0,12,0"/>
                        
                        <TextBlock Text="Событий: " Foreground="#6B7280" FontSize="11"/>
                        <TextBlock x:Name="EventsText" Text="0" FontWeight="Bold" Foreground="#374151" FontSize="11"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel x:Name="TimerPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0" Visibility="Collapsed">
                    <TextBlock Text="Авто-стоп через: " Foreground="#4B5563"/>
                    <TextBlock x:Name="TimerText" Text="60 с" FontWeight="Bold" Foreground="#DC2626"/>
                </StackPanel>
            </StackPanel>

            <!-- Buttons -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="ExtendButton"
                        Grid.Column="0" 
                        Content="Продлить" 
                        Background="#3B82F6" 
                        Foreground="White"
                        Click="Extend_Click"
                        Visibility="Collapsed"/>

                <Button x:Name="StopButton"
                        Grid.Column="1" 
                        Grid.ColumnSpan="2"
                        Content="Остановить" 
                        Background="#EF4444" 
                        Foreground="White"
                        Click="Stop_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
